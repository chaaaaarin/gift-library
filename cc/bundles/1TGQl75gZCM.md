# 【保存版】公式が発表したClaudeCodeを100倍賢くする、とっておきの裏技を徹底解説します — プレゼント一式
# 2026-07-28 配布 / 5点
# ==========================================================


### 1. グラフ化 要否4条件診断チェックリスト（チェックリスト）
# そのループ、本当にグラフ化が必要か。4つの質問で判定します。

# 🎁 グラフ化 要否4条件診断チェックリスト

そのループ、本当にグラフ化が必要か。4つの質問で判定します。

**受け取り方**: 下のコードブロック右上のコピーボタンでコピーするか、このファイルをそのままダウンロードしてください。

**使い方**: 今使っているAIループ（作る→レビュー→直す、を繰り返す仕組み）を1つ思い浮かべながら、上から順にチェックしてください。

**ポイント**: 2つ以上YESなら着手する価値あり。1つ以下ならまだループのままで十分です（無理にグラフ化しない）。

```text
【グラフ化 要否4条件診断チェックリスト】

■ ステップ1：視点を分けられるか確認する
□ 今のループは、役割の違う複数の視点（校閲・レビュー・チェック観点など）を1つのAIに同時にやらせていないか
→ YES: 視点ごとに独立したAI呼び出しに分割できないか検討する
指示例: このループを、視点ごとに分けるとしたら何個に分かれる？

■ ステップ2：並列化の余地を確認する
□ 本当は同時に進められる作業を、1つずつ順番にやらせていないか
→ YES: 並列実行（ファンアウト）の対象になる
指示例: このうち、同時に進めても問題ない工程はどれ？

■ ステップ3：制御フローを書き出せるか確認する
□ 「指摘・結果の種類によって戻り先が変わる」ような分岐が、その場の判断だけで決まっていないか
→ YES: 分岐図（グラフ）に書き出す価値がある
指示例: このループで、指摘の種類ごとに戻り先を分けるとしたら？

■ ステップ4：完了の基準そのものが変わったか確認する
□ グラフ化しても「何をもって終わりとするか」の基準は以前と同じままか
→ YES（同じまま）: 表面だけ複雑にしている可能性がある。基準も一緒に見直す
→ NO（基準も変えた）: 本質的にパラダイムが変わった可能性が高い

■ 判定
□ 2つ以上がYES → グラフ化の価値あり。まず1つのループに3行だけ足してみる
□ 1つ以下 → 今はループのままで十分。無理に複雑にしない
指示例: このループ、グラフ化すべきか判定して
```


### 2. グラフエンジニアリング実践プロンプト100選（プロンプト集）
# Claude CodeのDynamic Workflows・エージェントループに関する公式知識をあらかじめ埋め込んだプロンプト集です。10ジャンル×各10問、コピペするだけで、公式の定義・関数・制約を説明し直さずにグラフエンジニアリングを試せます。

# 🎁 グラフエンジニアリング実践プロンプト100選

Claude CodeのDynamic Workflows・エージェントループに関する**公式知識をあらかじめ埋め込んだ**プロンプト集です。10ジャンル×各10問、コピペするだけで、公式の定義・関数・制約を説明し直さずにグラフエンジニアリングを試せます。

**受け取り方**: 下のコードブロック右上のコピーボタンでコピーするか、このファイルをそのままダウンロードしてください。

**使い方**: そのままClaude Codeに貼り付けるだけです。プロンプトの前半（【公式知識】）は毎回同じ説明ブロックで、後半（【タスク】）だけが場面ごとに変わります。

**ポイント**: 公式知識のブロックがあるおかげで、Claudeに「ループとグラフの違い」や「Dynamic Workflowsの関数」を毎回説明し直す必要がありません。

---

## 診断：自分のループをグラフ化すべきか判定してもらう

### 1. 今の記事レビューの仕組みを見せるので、グラフ化すべき…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
今の記事レビューの仕組みを見せるので、グラフ化すべきか4つの観点（視点・並列化・制御フロー・完了基準）で判定して
```

### 2. このコーディングエージェントの使い方が、ループのまま…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
このコーディングエージェントの使い方が、ループのままで十分か教えて
```

### 3. 週次レポート作成の手順を伝えるので、並列化できる工程…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
週次レポート作成の手順を伝えるので、並列化できる工程を洗い出して
```

### 4. カスタマー対応フローを説明するので、役割の違う視点を…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
カスタマー対応フローを説明するので、役割の違う視点を分けられるか診断して
```

### 5. 今のリサーチのやり方を見せるので、制御フローを事前に…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
今のリサーチのやり方を見せるので、制御フローを事前に書き出せるか確認して
```

### 6. この承認プロセスは、グラフ化した方がいいのに未着手に…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
この承認プロセスは、グラフ化した方がいいのに未着手になっていないか教えて
```

### 7. 現状の校閲ループのままで、指摘の種類ごとに戻り先が分…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
現状の校閲ループのままで、指摘の種類ごとに戻り先が分かれているか点検して
```

### 8. このタスクは1回限りかどうかを判定し、グラフ化が過剰…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
このタスクは1回限りかどうかを判定し、グラフ化が過剰装備でないか確認して
```

### 9. 今のワークフローの完了基準が曖昧なままグラフ化しよう…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
今のワークフローの完了基準が曖昧なままグラフ化しようとしていないか確認して
```

### 10. 複数の担当が関わる業務フローを説明するので、グラフ化…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
複数の担当が関わる業務フローを説明するので、グラフ化の優先度を判定して
```

## 設計：Dynamic Workflowsスクリプトを書いてもらう

### 11. 複数の競合サイトを並列調査し、比較レポートに統合する…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
複数の競合サイトを並列調査し、比較レポートに統合するDynamic Workflowsスクリプトを書いて
```

### 12. 記事の初稿を作り、複数の観点でレビューしてから統合す…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
記事の初稿を作り、複数の観点でレビューしてから統合するDynamic Workflowsスクリプトを書いて
```

### 13. 3つの異なる切り口で同じ問題を検討させ、多数決で採用…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
3つの異なる切り口で同じ問題を検討させ、多数決で採用案を決めるDynamic Workflowsスクリプトを書いて
```

### 14. 大量のログファイルを並列で解析し、異常のみをまとめる…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
大量のログファイルを並列で解析し、異常のみをまとめるDynamic Workflowsスクリプトを書いて
```

### 15. 複数のSNS投稿案を同時生成し、トーンでフィルタして…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
複数のSNS投稿案を同時生成し、トーンでフィルタしてから1つに絞るDynamic Workflowsスクリプトを書いて
```

### 16. コードの変更点を機能追加・バグ修正・リファクタに自動…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
コードの変更点を機能追加・バグ修正・リファクタに自動分類してレビューを分岐させるスクリプトを書いて
```

### 17. 顧客の問い合わせを緊急度でルーティングし、対応テンプ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
顧客の問い合わせを緊急度でルーティングし、対応テンプレを出し分けるDynamic Workflowsスクリプトを書いて
```

### 18. 議事録から決定事項を抽出し、担当者ごとに振り分けるD…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
議事録から決定事項を抽出し、担当者ごとに振り分けるDynamic Workflowsスクリプトを書いて
```

### 19. 複数言語への翻訳を並列生成し、用語統一チェックを挟ん…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
複数言語への翻訳を並列生成し、用語統一チェックを挟んでから出力するスクリプトを書いて
```

### 20. 週次で複数指標を集計し、しきい値を超えたものだけレポ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
週次で複数指標を集計し、しきい値を超えたものだけレポートに残すDynamic Workflowsスクリプトを書いて
```

## リサーチ・調査の並列化

### 21. 新しいツールを導入する前に、公式ドキュメント・レビュ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
新しいツールを導入する前に、公式ドキュメント・レビュー記事・競合比較を並列で調べて1つにまとめて
```

### 22. 市場トレンドを、業界ニュース・SNSの反応・専門家の…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
市場トレンドを、業界ニュース・SNSの反応・専門家のブログの3方向から並列調査して
```

### 23. 技術選定のため、候補ライブラリ3つを同時に比較調査し…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
技術選定のため、候補ライブラリ3つを同時に比較調査して
```

### 24. 採用候補者の情報を、複数の公開情報源から並列で確認し…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
採用候補者の情報を、複数の公開情報源から並列で確認して
```

### 25. 新機能へのユーザー反応を、レビュー・SNS・問い合わ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
新機能へのユーザー反応を、レビュー・SNS・問い合わせの3ソースから並列に集めて
```

### 26. 競合の価格改定を、公式サイト・プレスリリース・ユーザ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
競合の価格改定を、公式サイト・プレスリリース・ユーザーの声から並列調査して
```

### 27. 規制動向を、政府発表・業界団体・専門家コメントの3系…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
規制動向を、政府発表・業界団体・専門家コメントの3系統で並列に追って
```

### 28. 出張先の情報を、交通・宿泊・現地事情の3観点で並列に…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
出張先の情報を、交通・宿泊・現地事情の3観点で並列に調べて
```

### 29. 新しい採用媒体を、費用対効果・評判・導入事例の3方向…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
新しい採用媒体を、費用対効果・評判・導入事例の3方向で並列比較して
```

### 30. サプライヤー候補を、価格・品質・納期実績の3軸で並列…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
サプライヤー候補を、価格・品質・納期実績の3軸で並列調査して
```

## レビュー・チェックの分岐グラフ化

### 31. 契約書ドラフトを、法務観点と事業観点の2つのレビュア…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
契約書ドラフトを、法務観点と事業観点の2つのレビュアーに分けて指摘を統合して
```

### 32. プレスリリース案を、事実確認担当と表現担当に分けてレ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
プレスリリース案を、事実確認担当と表現担当に分けてレビューさせて
```

### 33. デザイン案を、ブランド整合性担当と使いやすさ担当に分…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
デザイン案を、ブランド整合性担当と使いやすさ担当に分けてレビューさせて
```

### 34. 採用面接のフィードバックを、スキル評価担当とカルチャ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
採用面接のフィードバックを、スキル評価担当とカルチャーフィット担当に分けて集約して
```

### 35. 予算申請書を、金額の妥当性チェックと目的の妥当性チェ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
予算申請書を、金額の妥当性チェックと目的の妥当性チェックに分けてレビューして
```

### 36. プレゼン資料を、論理構成チェックとビジュアルチェック…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
プレゼン資料を、論理構成チェックとビジュアルチェックに分けてレビューして
```

### 37. 広告コピーを、法令順守チェックと訴求力チェックに分け…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
広告コピーを、法令順守チェックと訴求力チェックに分けてレビューして
```

### 38. 研修資料を、内容の正確性チェックと分かりやすさチェッ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
研修資料を、内容の正確性チェックと分かりやすさチェックに分けてレビューして
```

### 39. リリースノートを、技術的正確性チェックと読みやすさチ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
リリースノートを、技術的正確性チェックと読みやすさチェックに分けてレビューして
```

### 40. 社内規定改定案を、リスクチェックと運用のしやすさチェ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
社内規定改定案を、リスクチェックと運用のしやすさチェックに分けてレビューして
```

## コンテンツ制作（記事・資料・台本）のグラフ化

### 41. YouTube台本を、フック案を3パターン並列生成し…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
YouTube台本を、フック案を3パターン並列生成してから1つを選ぶ流れにして
```

### 42. ブログ記事を、リサーチ→執筆→ファクトチェックの3ノ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
ブログ記事を、リサーチ→執筆→ファクトチェックの3ノードに分け、事実誤認だけ差し戻す設計にして
```

### 43. メルマガを、複数の件名案を並列生成し、開封率が高そう…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
メルマガを、複数の件名案を並列生成し、開封率が高そうな1本を選ぶ流れにして
```

### 44. スライド資料を、章ごとに並列で下書きさせてから、通し…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
スライド資料を、章ごとに並列で下書きさせてから、通しで整合性チェックする流れにして
```

### 45. ポッドキャストの構成案を、複数のゲスト想定質問を並列…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
ポッドキャストの構成案を、複数のゲスト想定質問を並列生成してから絞り込む流れにして
```

### 46. 商品説明文を、機能訴求版とベネフィット訴求版を並列生…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
商品説明文を、機能訴求版とベネフィット訴求版を並列生成して比較する流れにして
```

### 47. 採用ページのコピーを、複数のペルソナ向けに並列生成し…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
採用ページのコピーを、複数のペルソナ向けに並列生成してから統合する流れにして
```

### 48. ホワイトペーパーを、章ごとの執筆を並列化し、最後に一…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
ホワイトペーパーを、章ごとの執筆を並列化し、最後に一貫したトーンへ整える流れにして
```

### 49. SNS投稿の構成案を、複数のスタイルで並列生成してか…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
SNS投稿の構成案を、複数のスタイルで並列生成してから選ぶ流れにして
```

### 50. イベントの告知文を、複数の切り口で並列生成し、ターゲ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
イベントの告知文を、複数の切り口で並列生成し、ターゲットに合わせて選ぶ流れにして
```

## 開発・コードレビューのグラフ化

### 51. プルリクエストのレビューを、バグ・設計・スタイルの3…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
プルリクエストのレビューを、バグ・設計・スタイルの3観点に自動振り分けして
```

### 52. リファクタ提案を、安全性チェックと可読性チェックの2…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
リファクタ提案を、安全性チェックと可読性チェックの2ノードに分けて統合して
```

### 53. 依存関係の更新を、破壊的変更チェックと軽微な変更で自…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
依存関係の更新を、破壊的変更チェックと軽微な変更で自動的に扱いを分けて
```

### 54. テストコードの生成を、正常系・異常系・境界値の3並列…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
テストコードの生成を、正常系・異常系・境界値の3並列タスクにして統合して
```

### 55. パフォーマンス改善案を、致命的なボトルネックとそれ以…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
パフォーマンス改善案を、致命的なボトルネックとそれ以外に自動振り分けして
```

### 56. セキュリティレビューを、重大な脆弱性とそれ以外の指摘…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
セキュリティレビューを、重大な脆弱性とそれ以外の指摘で対応フローを分けて
```

### 57. APIドキュメントの整備を、エンドポイントごとに並列…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
APIドキュメントの整備を、エンドポイントごとに並列生成してから統合して
```

### 58. マイグレーションスクリプトのレビューを、データ損失リ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
マイグレーションスクリプトのレビューを、データ損失リスクとそれ以外で扱いを分けて
```

### 59. エラーログの分類を、既知の問題と未知の問題に自動振り…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
エラーログの分類を、既知の問題と未知の問題に自動振り分けして対応を変えて
```

### 60. コードコメントの整備を、公開API向けと内部実装向け…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
コードコメントの整備を、公開API向けと内部実装向けで書き分けながら並列生成して
```

## カスタマー対応・問い合わせのグラフ化

### 61. 問い合わせを、既知のFAQで即答できるものと個別対応…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
問い合わせを、既知のFAQで即答できるものと個別対応が必要なものに振り分けて
```

### 62. クレーム対応を、緊急性が高いものと通常対応のものに振…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
クレーム対応を、緊急性が高いものと通常対応のものに振り分けて、対応方法を変えて
```

### 63. 解約申し出を、引き止め可能なケースと即対応すべきケー…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
解約申し出を、引き止め可能なケースと即対応すべきケースに振り分けて
```

### 64. サポートチケットを、技術的な問題とアカウント関連の問…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
サポートチケットを、技術的な問題とアカウント関連の問題に振り分けて担当を分けて
```

### 65. アンケート回答を、ポジティブ・ネガティブ・中立に振り…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
アンケート回答を、ポジティブ・ネガティブ・中立に振り分けて対応を変えて
```

### 66. 問い合わせの一次回答を、事実確認が必要なものとテンプ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
問い合わせの一次回答を、事実確認が必要なものとテンプレで返せるものに振り分けて
```

### 67. 返金依頼を、規定内のものと個別判断が必要なものに振り…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
返金依頼を、規定内のものと個別判断が必要なものに振り分けて
```

### 68. 新規問い合わせを、営業対応が必要なものとサポート対応…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
新規問い合わせを、営業対応が必要なものとサポート対応でよいものに振り分けて
```

### 69. SNS上の言及を、直接返信が必要なものとモニタリング…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
SNS上の言及を、直接返信が必要なものとモニタリングだけでよいものに振り分けて
```

### 70. 紹介・パートナー問い合わせを、緊急度と重要度の2軸で…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
紹介・パートナー問い合わせを、緊急度と重要度の2軸で振り分けて対応順を決めて
```

## 社内業務・事務処理のグラフ化

### 71. 経費精算を、規定内のものと要確認のものに自動振り分け…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
経費精算を、規定内のものと要確認のものに自動振り分けして
```

### 72. 稟議書を、承認要件が揃っているものと差し戻しが必要な…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
稟議書を、承認要件が揃っているものと差し戻しが必要なものに振り分けて
```

### 73. 勤怠データを、通常のものと異常値（打刻漏れ等）に振り…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
勤怠データを、通常のものと異常値（打刻漏れ等）に振り分けて確認フローを分けて
```

### 74. 契約更新リマインドを、緊急度別に振り分けて担当者への…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
契約更新リマインドを、緊急度別に振り分けて担当者への通知方法を変えて
```

### 75. 社内問い合わせ対応記録を、個人情報を含むものと含まな…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
社内問い合わせ対応記録を、個人情報を含むものと含まないもので扱いを分けて
```

### 76. 備品発注申請を、予算内のものと予算超過のもので承認フ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
備品発注申請を、予算内のものと予算超過のもので承認フローを分けて
```

### 77. 新入社員のオンボーディング資料を、部署ごとに並列生成…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
新入社員のオンボーディング資料を、部署ごとに並列生成してから統合して
```

### 78. 会議の議事録から、即対応が必要なアクションとそうでな…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
会議の議事録から、即対応が必要なアクションとそうでないものを振り分けて
```

### 79. 社内アンケートの自由記述を、要対応の意見とそうでない…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
社内アンケートの自由記述を、要対応の意見とそうでない意見に振り分けて
```

### 80. 監査対応の証跡集めを、複数部門から並列で集めてから1…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
監査対応の証跡集めを、複数部門から並列で集めてから1つに統合して
```

## 検証・対立的レビュー（Adversarial Verification）の実装

### 81. この結論案に対して、複数の懐疑的なレビュアーを立てて…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
この結論案に対して、複数の懐疑的なレビュアーを立てて反論を集めてから採用可否を判定して
```

### 82. この市場予測に対して、楽観シナリオと悲観シナリオの両…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
この市場予測に対して、楽観シナリオと悲観シナリオの両方から検証させて
```

### 83. この採用判断に対して、賛成理由と反対理由を別々に出さ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
この採用判断に対して、賛成理由と反対理由を別々に出させてから統合して
```

### 84. この価格戦略に対して、顧客視点と収益視点の両方から批…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
この価格戦略に対して、顧客視点と収益視点の両方から批判的にレビューさせて
```

### 85. この技術選定に対して、推進派と慎重派の両方の意見を出…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
この技術選定に対して、推進派と慎重派の両方の意見を出させてから判断材料にして
```

### 86. このリスク評価に対して、過小評価していないか複数の視…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
このリスク評価に対して、過小評価していないか複数の視点で検証させて
```

### 87. このコンテンツの主張に対して、事実に基づく反証を探す…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
このコンテンツの主張に対して、事実に基づく反証を探すレビュアーを立てて
```

### 88. この事業計画に対して、楽観的な前提を疑う役割のレビュ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
この事業計画に対して、楽観的な前提を疑う役割のレビュアーを立てて
```

### 89. このアンケート結果の解釈に対して、別の解釈がないか対…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
このアンケート結果の解釈に対して、別の解釈がないか対立的に検証させて
```

### 90. この提案の実現可能性に対して、否定的な立場からのレビ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
この提案の実現可能性に対して、否定的な立場からのレビューを必ず1回挟んで
```

## 振り返り・改善：既存の仕組みにアンカーを追加する

### 91. このループが追っている指標が、いつの間にか本来の目的…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
このループが追っている指標が、いつの間にか本来の目的とズレていないか点検して
```

### 92. このAI運用の目標値自体が、今も正しいままか疑ってみ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
このAI運用の目標値自体が、今も正しいままか疑ってみて
```

### 93. 複数のループが競合していないか、業務全体を俯瞰して点…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
複数のループが競合していないか、業務全体を俯瞰して点検して
```

### 94. このループが参照しているデータが古くなっていないか確…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
このループが参照しているデータが古くなっていないか確認して
```

### 95. この仕組みに、変更できない人間のルール（アンカー）が…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
この仕組みに、変更できない人間のルール（アンカー）が足りているか点検して
```

### 96. この自動化フローに、最終的な人間の判断ポイントが残っ…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
この自動化フローに、最終的な人間の判断ポイントが残っているか確認して
```

### 97. この指標を最適化した結果、別の指標が悪化していないか…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
この指標を最適化した結果、別の指標が悪化していないか確認して
```

### 98. このループの「完了」の定義が、今も実態に合っているか…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
このループの「完了」の定義が、今も実態に合っているか点検して
```

### 99. この仕組みが「筋は通っているが検証されていない」状態…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
この仕組みが「筋は通っているが検証されていない」状態になっていないか確認して
```

### 100. 半年前に設計したこのループを、今の状況に合わせて見直…
```text
【公式知識】
・ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、指摘・結果の種類ごとに戻り先を変える仕組み。
・Claude CodeのDynamic Workflowsの3つの基本関数：agent(prompt, opts) は単体のサブエージェントを1つ実行、parallel(thunks) は全部そろうまで待つバリア付き並列実行、pipeline(items, ...stages) はバリアなしで段階的に流す処理。
・並列実行数は同時16体・累計1,000体が上限（報告値）。Date.now()やMath.random()などの非決定論的コードは実行時エラーになる設計（再現性のため）。
・Claude Code作者Boris Cherny氏は「上位レベルに進むには /loop・/batch・dynamic workflows、そしてサブエージェント用のworktree隔離を使う」と述べている。

【タスク】
半年前に設計したこのループを、今の状況に合わせて見直すべきか判定して
```

---

以上、100個です。10ジャンル×各10問。


### 3. Dynamic Workflows実装ライター SKILL.md（設定・スニペット）
# グラフ化の設計が決まったら、Claude CodeのDynamic Workflows形式（agent/parallel/pipeline）で実装コードまで書いてくれるSkillです。

# 🎁 Dynamic Workflows実装ライター SKILL.md

グラフ化の設計が決まったら、Claude CodeのDynamic Workflows形式（agent/parallel/pipeline）で実装コードまで書いてくれるSkillです。

**受け取り方**: 下のコードブロック右上のコピーボタンでコピーするか、このファイルをそのままダウンロードしてください。

**使い方**: `.claude/skills/dynamic-workflow-writer/SKILL.md` として保存してください。「〇〇を並列でやるワークフローを書いて」のように頼むと自動的に呼び出されます。

```text
---
description: グラフ化の設計（並列化したい処理・分岐条件）が決まったタスクに対して、Claude CodeのDynamic Workflows形式（agent()/parallel()/pipeline()）でJavaScriptオーケストレーションスクリプトを書く。「ワークフローを書いて」「並列で処理するスクリプトを作って」等の依頼で使う。
---

# Dynamic Workflows実装ライター

あなたはClaude CodeのDynamic Workflowsの公式仕様に従って、オーケストレーションスクリプトを書く担当です。

## 使えるプリミティブ
- `agent(prompt, opts)` — 単体のサブエージェントを1つ実行。`schema`オプションで構造化出力を強制できる
- `parallel(thunks)` — バリア付き並列実行。渡した全タスクがそろうまで待ってから次に進む。ファンアウトに使う
- `pipeline(items, ...stages)` — バリアなしの段階的処理。アイテムごとに独立して段階を進められる

## 守るべき制約
- **非決定論的コードを書かない**：`Date.now()`・`Math.random()`・引数なしの`new Date()`は使わない（実行がジャーナリングされ再開可能にするため、実行時エラーになる設計）。日時が必要な場合は引数（`args`）で外から渡す
- **並列数の目安**：同時16体・累計1,000体が上限と報告されている。大量ファンアウトを書くときはこの範囲に収まるよう分割する
- **バリアは必要な箇所だけ**：デフォルトは`pipeline()`を使い、1つの段階が全ての前段階の結果を同時に必要とする場合だけ`parallel()`のバリアを使う

## 書き方の手順
1. ユーザーの依頼から「並列化したい処理」「統合・集約の仕方」「最終的に欲しい出力」を確認する
2. `export const meta = { name, description }` を先頭に書く
3. ファンアウト（`parallel`）→ リデュース（普通のJS処理）→ シンセサイズ（最後に1回`agent()`で統合）の骨格を基本形にする
4. 分岐が必要な場合は、ラベルに応じて呼び出す`agent()`のプロンプトを条件分岐で切り替える
5. 書いたスクリプトの末尾に、想定される実行結果の形（返り値の例）を1行コメントで添える

## 注意点
- 検証パスが必要な場面（対立的検証・複数レンズ検証）では、作る役のプロンプトと検証する役のプロンプトを別の`agent()`呼び出しに分離し、検証役には作る役の思考過程を渡さない
- トークン消費が大きい設計なので、単発タスクや人間の判断が必要な場面には使わないよう一言添える
```


### 4. グラフ化アドバイザー SKILL.md（設定・スニペット）
# あなたが説明する既存のAIループ・業務フローに対して、公式知識に基づいて「グラフ化すべきか」を判定し、必要なら分岐の設計案まで提案してくれるSkillです。

# 🎁 グラフ化アドバイザー SKILL.md

あなたが説明する既存のAIループ・業務フローに対して、公式知識に基づいて「グラフ化すべきか」を判定し、必要なら分岐の設計案まで提案してくれるSkillです。

**受け取り方**: 下のコードブロック右上のコピーボタンでコピーするか、このファイルをそのままダウンロードしてください。

**使い方**: `.claude/skills/graph-engineering-advisor/SKILL.md`（個人用なら `~/.claude/skills/`）として保存してください。以降、Claude Codeに「このループ、グラフ化すべき？」と聞くだけで自動的に呼び出されます。

```text
---
description: ユーザーが説明する既存のAIループ・業務フロー・レビュー体制に対して、グラフ化すべきかどうかを公式知識に基づいて診断し、必要なら分岐（グラフ化）の設計案を提示する。「グラフ化すべき？」「ループのままでいい？」「このループ、見直して」等の依頼で使う。
---

# グラフ化アドバイザー

あなたはClaude CodeのDynamic Workflows・エージェントループに関する公式知識を踏まえて、ユーザーの既存の仕組みを診断するアドバイザーです。

## 前提知識
- ループ＝1つのAIが「発見→計画→実行→検証」を繰り返す1本の輪。グラフ＝複数のノード（ループ）を配線でつなぎ、結果の種類ごとに戻り先を変える仕組み。
- グラフの中の1つひとつのノードも、相変わらずループで動いている。グラフはループを置き換えるのではなく、上位レイヤーとして重なる。
- Dynamic Workflowsの3関数：`agent()`＝単体実行、`parallel()`＝バリアあり並列（全部そろうまで待つ）、`pipeline()`＝バリアなし段階処理。

## 診断手順
ユーザーが仕組みを説明したら、次の4条件を順に確認する。

1. **視点を分けられるか**——役割の違う複数の視点を1つのAIに同時にやらせていないか
2. **並列化の余地があるか**——本当は同時に進められる作業を、順番にやらせていないか
3. **制御フローを書き出せるか**——「結果の種類によって戻り先が変わる」分岐が、その場の判断だけで決まっていないか
4. **完了の基準そのものが変わるか**——グラフ化しても「終わり」の基準は以前と同じままか（同じなら表面だけ複雑にしている可能性を指摘する）

## 判定と提案
- 2つ以上がYESなら「グラフ化の価値あり」と判定し、具体的な分岐ラベル案（例：【再検証】【構成】【局所】）と、それぞれの戻り先・打ち切り条件を提案する
- 1つ以下なら「今はループのままで十分」と伝え、無理にグラフ化しないよう助言する
- 診断結果は必ず理由つきで、ユーザーが自分の言葉で人に説明できる形にする

## 注意点
- グラフ化しても、外部の実際の成果・変更できないルール・人間の最終判断という「アンカー」がなければ、同じ失敗を繰り返しうる点も併せて伝える
- 非決定論的な処理（現在時刻・乱数）はDynamic Workflows内では使えない設計である点も、実装段階の話が出たら伝える
```


### 5. 定期レビュアー SKILL.md（設定・スニペット）
# 運用中のループ・グラフを定期的に振り返り、指標の形骸化などの失敗パターンと「アンカー」の有無を点検してくれるSkillです。

# 🎁 定期レビュアー SKILL.md

運用中のループ・グラフを定期的に振り返り、指標の形骸化などの失敗パターンと「アンカー」の有無を点検してくれるSkillです。

**受け取り方**: 下のコードブロック右上のコピーボタンでコピーするか、このファイルをそのままダウンロードしてください。

**使い方**: `.claude/skills/loop-graph-reviewer/SKILL.md` として保存してください。「このループ、見直して」「劣化してないか点検して」等の依頼で自動的に呼び出されます。

```text
---
description: 運用中のAIループ・グラフ化した仕組みを定期的に振り返り、指標の形骸化・目標の妥当性・複数ループの競合・データの劣化という4つの失敗パターンと、外部アンカーの有無を点検する。「このループ見直して」「劣化してないか点検して」「運用を振り返って」等の依頼で使う。
---

# 定期レビュアー

あなたは、運用中のAIループ・グラフの健全性を定期的に点検する担当です。

## 点検する4つの失敗パターン
1. **指標の形骸化（Goodhartの法則）**——追いかけている指標が、いつの間にか本来の目的を裏切っていないか。数字は良くなっているのに、本質的な成果が悪化していないか確認する
2. **目標値自体を疑えているか**——そのループが向かっている目標値は、今も正しいか。設定した当時から状況が変わっていないか確認する
3. **複数ループの競合**——独立に作られた複数のループ・仕組みが、互いの足を引っ張り合っていないか（例：速度を追う仕組みと、丁寧さを追う仕組みが対立していないか）
4. **測定の劣化**——参照しているデータやセンサーが古びていないか。判断の元になっている情報が現実とズレていないか

## アンカーの確認
グラフ化・複数ループ化していても、以下の「アンカー」が無ければ同じ失敗を繰り返す。それぞれ有無を確認する。
- 外部で実際に検証できる成果（実際の数字・実際に起きた結果）
- 最適化のループが勝手に書き換えられない、変更不可のルール
- 人間の最終判断が入るポイントが、どこかに残っているか

## 進め方
1. ユーザーに運用中の仕組みを説明してもらう（指標・頻度・関わる人・最近の変化）
2. 4つの失敗パターンを1つずつ具体的な質問で確認する（抽象論で終わらせない）
3. アンカーの3項目を確認する
4. 見つかった懸念点を優先度つきでまとめ、次にとるべき具体的なアクションを1〜3個提案する

## 注意点
- 「問題なし」と結論づける場合も、何を確認してそう判断したかを明記する
- 深刻な懸念（人間の判断ポイントが完全に無い等）が見つかった場合は、それを最優先で提示する
```

# 【神モデル】ClaudeCodeを100倍超えるGemini 3.7 Flashが登場しました — プレゼント一式
# 2026-08-26 配布 / 3点
# ==========================================================


### 1. タスク別コスト削減シミュレーションシート（テンプレート）
# Gemini 3.7 Flashへの分業で、月々のAI活用コストがどれだけ変わるかを試算するためのチェックリストです。2027年1月の価格改定後も見据えた設計になっています。

# 🎁 タスク別コスト削減シミュレーションシート

Gemini 3.7 Flashへの分業で、月々のAI活用コストがどれだけ変わるかを試算するためのチェックリストです。2027年1月の価格改定後も見据えた設計になっています。

**受け取り方**: 下のコードブロック右上のコピーボタンでコピーするか、このファイルをそのままダウンロードしてください。

**使い方**: 上から順に埋めていくと、分業を続けるべきかどうかの判断材料が揃います。

```text
【タスク別コスト削減シミュレーションシート】

■ ステップ1：現在の月間タスク量を棚卸しする
□ Claude Codeで処理している月間タスク数を把握しているか
→ YES: 月間タスク数を記録する（例: 500件）
□ 1タスクあたりの平均入力/出力トークン数が分かるか
→ YES: 分かる範囲で概算値を記録する（不明なら入力1000/出力500トークンを仮置きにする）
指示例: 直近1ヶ月分のAPI利用ログを確認し、月間タスク数と平均トークン数を集計してください。

■ ステップ2：分業できるタスクを仕分ける
□ そのタスクは「量が多く、単純な処理（抽出・変換・整形等）」か
→ YES: Gemini 3.7 Flashへの委譲候補としてリストアップする
□ そのタスクは「複雑な判断・長文出力・Claude Codeへの深い統合」を伴うか
→ YES: 引き続きClaude Sonnet 5/Opus 5に残す候補としてリストアップする
指示例: 現在のワークフローの各ステップを一覧化し、上記2つの基準でGemini向き/Claude向きに仕分けてください。

■ ステップ3：現状コストを試算する（2026年内・導入価格ベース）
□ Claude Sonnet 5のみで全タスクを処理した場合の月額を計算したか
→ YES: 月間タスク数 × 平均トークン数 ×（入力$2.00/出力$10.00 per 1M）で概算する
指示例: 月間タスク数と平均トークン数から、Claude Sonnet 5のみで運用した場合の月額費用を試算してください。

■ ステップ4：分業後のコストを試算する（2026年内・導入価格ベース）
□ Gemini向きタスクをGemini 3.7 Flash（入力$0.75/出力$3.75 per 1M）に置き換えた場合の月額を計算したか
→ YES: Gemini分のコスト＋Claude残タスク分のコストを合算する
指示例: ステップ2で仕分けたGemini向きタスクの分だけ、Gemini 3.7 Flashの導入価格で再計算し、Claude残分と合算した月額を出してください。

■ ステップ5：2027年1月以降の価格改定を織り込む
□ Gemini 3.7 Flashの通常価格（入力$1.50/出力$7.50 per 1M、2027年1月1日〜）で再計算したか
→ YES: ステップ4の試算をこの単価に置き換えて再計算する
指示例: 2027年1月以降の通常価格を前提に、分業後の月額コストを再試算してください。

■ ステップ6：損益分岐点を確認する
□ 2027年以降の分業コストが、Claude単独運用コストを上回っていないか
→ YES（上回っていない）: 分業体制を継続する価値ありと判断する
→ NO（上回っている）: 分業対象タスクの見直し、またはワークフロー設計自体の再検討を行う
指示例: ステップ3の単独運用コストと、ステップ5の2027年以降コストを比較し、分業を継続すべきか判定してください。

■ ステップ7：定期的な見直しサイクルを決める
□ この試算を毎月／四半期ごとなど、定期的に見直す頻度を決めたか
→ YES: カレンダーに見直しタイミングを登録する
指示例: このコスト試算シートを使った見直しを、四半期に1回のペースでリマインドするタスクを作成してください。
```


### 2. Gemini 3.7 Flash × Claude Code 実践プロンプト60選（プロンプト集）
# Gemini 3.7 FlashとClaude Codeを併用するワークフローで、そのまま貼って使える実践プロンプト集です。6カテゴリ×各10本、全60本。

# 🎁 Gemini 3.7 Flash × Claude Code 実践プロンプト60選

Gemini 3.7 FlashとClaude Codeを併用するワークフローで、そのまま貼って使える実践プロンプト集です。6カテゴリ×各10本、全60本。

**受け取り方**: 下の各コードブロック右上のコピーボタンでコピーするか、このファイルをそのままダウンロードしてください。

**使い方**: 対象のAIツールのチャット欄やCLIにそのまま貼り付け、〈 〉部分をご自身のタスク内容に置き換えてお使いください。

## 大量処理・一括変換系

### 1. フォーマット統一の一括変換

```text
前提: 表記ゆれのある大量のMarkdownファイルを1つのプロジェクトで管理している。
指示: 指定フォルダ配下のMarkdownファイルすべてに対し、見出しレベル・箇条書き記号・コードブロック言語タグの表記を統一して。
出力形式: 変更したファイル一覧と、各ファイルの変更箇所数を表で報告して。差分は変更前後を並べず、要約のみでよい。
```

### 2. 大量画像のファイル名正規化

```text
前提: 数百枚の画像ファイルが作成日時バラバラのファイル名で保存されている。
指示: 指定フォルダの画像ファイルをEXIF情報から作成日時を読み取り、YYYYMMDD_連番の形式にリネームして。
出力形式: リネーム前後の対応表をCSVで出力し、EXIF情報が読めなかったファイルは別リストにまとめて。
```

### 3. CSVの列名・単位統一

```text
前提: 複数の担当者が別々に作ったCSVファイルが列名・単位バラバラで存在する。
指示: 指定フォルダのCSVすべてを読み込み、列名と単位（円/万円、個/件等）を1つの基準に統一して結合して。
出力形式: 統一後の結合CSVと、各ファイルで何を変換したかのログを別ファイルで出力して。
```

### 4. 大量テキストの要約バッチ処理

```text
前提: 100件を超えるアンケート自由記述回答をざっと把握したい。
指示: 指定フォルダのテキストファイルを1件ずつ読み込み、それぞれ1行で要点をまとめて。
出力形式: 元ファイル名・要約1行・ポジティブ/ネガティブ/中立の3値分類を持つ表にして出力して。
```

### 5. 重複ファイルの検出と整理案

```text
前提: ダウンロードフォルダに似た名前のファイルが大量に溜まっている。
指示: 指定フォルダ内のファイルをハッシュ値と内容の類似度で比較し、重複候補をグループ化して。
出力形式: グループごとに残すべき1件（最新更新日）と削除候補を分けたリストで出力して。実削除はしない。
```

### 6. 大量ログファイルのエラー抽出

```text
前提: 日次で出力される大量のログファイルからエラーだけを追いたい。
指示: 指定フォルダのログファイル全件からERROR/WARNレベルの行だけを抽出して。
出力形式: 発生時刻・ログレベル・メッセージ要約を持つ表にまとめ、同一パターンのエラーは件数で集計して。
```

### 7. 大量スクリーンショットのOCR一括処理

```text
前提: 手順書用に撮った大量のスクリーンショットから、文字情報だけ抜き出したい。
指示: 指定フォルダの画像すべてに対しOCRを実行し、含まれるテキストを抽出して。
出力形式: 画像ファイル名ごとに抽出テキストをMarkdownの見出し+本文形式でまとめて出力して。
```

### 8. 大量音声メモの文字起こし振り分け

```text
前提: 会議の音声メモが複数件たまっていて、内容ごとに仕分けたい。
指示: 指定フォルダの音声ファイルを文字起こしし、内容から「決定事項」「TODO」「雑談」に自動分類して。
出力形式: ファイルごとに分類結果とその根拠1文を添えた表を出力して。
```

### 9. 大量PDFのメタデータ一括整理

```text
前提: 契約書PDFが大量にあり、タイトルや日付がファイル名から分からない。
指示: 指定フォルダのPDFを開き、契約当事者・契約日・契約種別をメタデータとして抽出して。
出力形式: ファイル名・契約当事者・契約日・契約種別の4列を持つ一覧表をCSVで出力して。
```

### 10. 多言語ファイルの一括翻訳下書き

```text
前提: 英語のドキュメントが大量にあり、まず日本語の下書きが欲しい。
指示: 指定フォルダの英語Markdownファイルすべてを日本語に翻訳し、原文と対訳で並べて。
出力形式: 見出し構造は維持したまま、段落ごとに原文→訳文の順で出力して。専門用語は英語併記で残して。
```

## PDF・文書処理系

### 11. 請求書PDFからのデータ抽出

```text
前提: 毎月大量の請求書PDFが届き、経理システムへの手入力に時間がかかっている。
指示: 指定フォルダの請求書PDFすべてから、発行元・金額・支払期日・請求番号を抽出して。
出力形式: 経理システムに取り込めるCSV形式（列順固定）で出力し、抽出できなかった項目はNULLと明記して。
```

### 12. 契約書PDFの重要条項ハイライト

```text
前提: 契約書の中から、確認すべき条項だけを素早く把握したい。
指示: 指定PDFから契約期間・解約条件・違約金・自動更新の有無に関する条項を抽出して。
出力形式: 条項名・原文引用・ページ番号の3列を持つ表で出力して。該当条項が無い場合はその旨明記して。
```

### 13. 複数レポートPDFの数値横断比較

```text
前提: 四半期ごとのレポートPDFが複数あり、推移を追いたい。
指示: 指定フォルダの複数PDFから同一指標（売上・利益率等）を抽出し、時系列で並べて。
出力形式: 指標名を行、四半期を列にした比較表を作成し、前期比の増減率も併記して。
```

### 14. PDFフォームの入力内容一括読み取り

```text
前提: 紙のアンケートをスキャンしたPDFが大量にある。
指示: 指定フォルダのPDFから、記入されたチェック項目と自由記述欄の内容を読み取って。
出力形式: 回答者ごとに1行の表形式で、質問項目を列にして出力して。判読不能な箇所は「要確認」と記載して。
```

### 15. 長文レポートの経営層向け要約

```text
前提: 30ページを超える調査レポートPDFを、経営層に3分で説明したい。
指示: 指定PDFの全体を読み、結論・根拠データ・推奨アクションの3段構成で要約して。
出力形式: 各段落200字以内、合計600字程度に収め、原文のページ番号を根拠として併記して。
```

### 16. PDFの表データをそのままスプレッドシート化

```text
前提: PDF内の複雑な表を、そのまま集計に使いたい。
指示: 指定PDF内の表を、セル結合や罫線の崩れを補正しながらテーブルデータとして抽出して。
出力形式: 抽出結果をCSVで出力し、元の表と列数が一致しない箇所があれば理由とともに報告して。
```

### 17. PDF規約の変更点差分抽出

```text
前提: 利用規約が改定され、旧版と新版のPDFがある。
指示: 旧版PDFと新版PDFを比較し、追加・削除・変更された条項だけを抽出して。
出力形式: 変更種別（追加/削除/変更）・条項名・旧文・新文の4列で表にまとめて。
```

### 18. PDF論文の手法セクションだけ抽出

```text
前提: 大量の論文PDFから、手法（Method）部分だけを効率よく読みたい。
指示: 指定フォルダの論文PDFすべてから手法セクションを特定し、日本語で要約して。
出力形式: 論文タイトル・著者・手法要約（300字以内）を持つ一覧表で出力して。
```

### 19. PDFマニュアルのFAQ自動生成

```text
前提: 製品マニュアルPDFがあり、そこからFAQページを作りたい。
指示: 指定PDFの内容から、想定される質問とその回答を抽出・生成して。
出力形式: Q&A形式で最低15組作成し、根拠となったページ番号を各回答に添えて。
```

### 20. 大量PDFの言語別振り分け

```text
前提: 多言語のPDF資料が混在するフォルダを整理したい。
指示: 指定フォルダのPDFすべてを開き、使用言語を判定してフォルダ分けの提案を出して。
出力形式: ファイル名・判定言語・信頼度（高中低）の一覧表を出力して。実際のファイル移動はしない。
```

## Web・UI生成系

### 21. スクリーンショットからのUI再現

```text
前提: 参考にしたいWebサイトのスクリーンショットがある。
指示: 添付スクリーンショットを見て、同じレイアウト構成のHTML/CSSたたき台を生成して。
出力形式: 単一HTMLファイルで、色・フォント・余白の数値をコメントで明記して。使用フォントは代替案も添えて。
```

### 22. ランディングページの複数バリエーション生成

```text
前提: A/Bテスト用に見出し・配色違いのLPを複数パターン用意したい。
指示: 指定した訴求ポイントをもとに、配色3パターン・見出し3パターンの組み合わせでLPたたき台を生成して。
出力形式: パターンごとに別ファイルで出力し、ファイル名にパターン内容が分かる接尾辞を付けて。
```

### 23. 既存サイトのレスポンシブ化診断

```text
前提: PC向けに作ったページをスマホでも見やすくしたい。
指示: 指定HTMLを解析し、スマホ幅（375px）で崩れる箇所を特定して修正案を出して。
出力形式: 崩れ箇所・原因・修正後のCSSスニペットを箇条書きで並べて。
```

### 24. フォームUIのバリデーション付き生成

```text
前提: 問い合わせフォームを新規に作りたい。
指示: 氏名・メール・本文の3項目を持つフォームを、入力チェック（必須・メール形式）付きで生成して。
出力形式: 単一HTMLで、JavaScriptのバリデーション処理も含めて。エラーメッセージは日本語で。
```

### 25. 既存デザインシステムへの準拠チェック

```text
前提: 新しく作ったコンポーネントが、既存のデザインシステムに沿っているか確認したい。
指示: 指定のCSS変数一覧と、新規コンポーネントのHTML/CSSを照合し、逸脱箇所を洗い出して。
出力形式: 逸脱箇所・使用すべき正しい変数名の対応表を出力して。
```

### 26. ダークモード対応の一括追加

```text
前提: ライトモードのみのページに、ダークモード対応を追加したい。
指示: 指定HTMLの色指定をCSS変数化し、prefers-color-schemeによるダークモード切り替えを追加して。
出力形式: 変更後のCSS全文と、変更箇所リストを分けて出力して。
```

### 27. 大量アイコンのSVG line-art統一生成

```text
前提: バラバラな画風のアイコンを、統一されたline-artに揃えたい。
指示: 指定した概念リスト（例: 検索・設定・通知など）ごとに、stroke統一のシンプルなSVGアイコンを生成して。
出力形式: 概念名とSVGコードの対応をMarkdownのコードブロックで並べて出力して。
```

### 28. ダミーデータを使ったUIプロトタイプ生成

```text
前提: バックエンドがまだ無い状態で、UIの見た目だけ先に確認したい。
指示: 指定した項目名リストをもとに、それらしいダミーデータを含む一覧画面のHTMLを生成して。
出力形式: 単一HTMLで、ダミーデータは配列として先頭にまとめ、後から差し替えやすい形にして。
```

### 29. アクセシビリティの機械チェックと修正提案

```text
前提: 既存ページがアクセシビリティ基準を満たしているか不安。
指示: 指定HTMLをコントラスト比・alt属性・見出し構造の観点でチェックし、問題点を洗い出して。
出力形式: 問題箇所・基準に対する不足内容・修正済みコードスニペットの3列で表にまとめて。
```

### 30. 複数ページの共通ヘッダー/フッター統一

```text
前提: 複数のHTMLファイルでヘッダー・フッターがバラバラになっている。
指示: 指定フォルダのHTMLすべてを確認し、共通のヘッダー/フッターに統一する変更を適用して。
出力形式: 変更したファイル一覧と、統一後のヘッダー/フッターのコードを分けて出力して。
```

## 自動化ワークフロー系

### 31. 日次レポートの自動集計フロー

```text
前提: 毎朝、複数のCSVから同じ指標を集計してSlackに投稿している。
指示: 指定フォルダの最新CSVから前日分の指標を集計し、投稿用のテキストを生成して。
出力形式: Slack投稿用のMarkdown形式テキストを出力し、数値は前日比の記号（↑↓→）付きで。
```

### 32. 問い合わせメールの一次仕分け

```text
前提: 問い合わせメールが1日数十件届き、緊急度で仕分けたい。
指示: 指定した複数の問い合わせ文面を読み、緊急度（高中低）とカテゴリ（技術/請求/その他）に分類して。
出力形式: 件名・緊急度・カテゴリ・分類理由1行を持つ表で出力して。
```

### 33. 週次ニュースまとめの自動生成

```text
前提: 業界ニュースを毎週まとめて共有している。
指示: 指定した複数のニュース記事URLの内容から要点を抽出し、週次まとめの下書きを作成して。
出力形式: 記事ごとに見出し・3行要約・出典リンクを持つMarkdownリストで出力して。
```

### 34. 在庫データの異常値検知バッチ

```text
前提: 在庫管理表を毎日チェックし、異常な増減を見つけたい。
指示: 指定した在庫CSVの前日分と本日分を比較し、通常の変動幅を超えた品目を抽出して。
出力形式: 品目名・前日数量・本日数量・変動率の表を出力し、変動率の大きい順に並べて。
```

### 35. 社内ドキュメントの棚卸しチェック

```text
前提: 社内Wikiのドキュメントが古くなっていないか定期的に確認したい。
指示: 指定フォルダのMarkdownドキュメントの最終更新日と内容を確認し、更新が必要そうなものを抽出して。
出力形式: ファイル名・最終更新日・更新が必要と判断した理由の表で出力して。
```

### 36. SNS投稿文の複数パターン一括生成

```text
前提: 同じ告知を複数のSNSに合わせた文体で投稿したい。
指示: 指定した告知内容から、X向け（140字以内）とInstagram向け（絵文字多め）の投稿文をそれぞれ生成して。
出力形式: プラットフォーム名ごとに投稿文を分けて出力し、文字数をカッコ書きで添えて。
```

### 37. 会議メモからのタスク自動抽出

```text
前提: 会議の議事録から、誰が何をいつまでにやるかを毎回手作業で拾い出している。
指示: 指定した議事録テキストから、担当者・タスク内容・期限の組み合わせを抽出して。
出力形式: 担当者・タスク・期限の3列を持つ表で出力し、期限が明記されていないものは「未定」と記載して。
```

### 38. 複数拠点データの統合レポート化

```text
前提: 拠点ごとに別フォーマットの実績データが上がってくる。
指示: 指定フォルダの拠点別データを共通フォーマットに変換し、全社統合レポートとしてまとめて。
出力形式: 拠点名を行、共通指標を列にした統合表を出力し、フォーマット変換で欠損した項目は注記して。
```

### 39. 定型返信文のカスタマイズ自動生成

```text
前提: よくある問い合わせに対し、毎回似た内容だが少し違う返信を書いている。
指示: 指定したテンプレート文と、個別の問い合わせ内容をもとに、カスタマイズされた返信文を生成して。
出力形式: 返信文のみを出力し、テンプレートから変更した箇所には理由をコメントとして添えて。
```

### 40. 進捗管理表の自動ステータス更新案

```text
前提: プロジェクトの進捗管理表を、日々の作業ログから手動更新している。
指示: 指定した作業ログの内容から、各タスクのステータス（未着手/進行中/完了）の更新案を出して。
出力形式: タスク名・現在のステータス・更新後ステータス・根拠となったログ箇所の表で出力して。
```

## コスト管理・仕分け系

### 41. タスクの難易度による振り分け判定

```text
前提: Claude CodeとGemini 3.7 Flashのどちらに任せるか毎回悩んでいる。
指示: 指定したタスク一覧それぞれについて、判断の複雑さと処理量の観点からどちらのモデルが適するか判定して。
出力形式: タスク名・推奨モデル・判定理由1行の表で出力して。迷う場合は「要検討」と明記して。
```

### 42. API利用量の月次コスト試算

```text
前提: 現在のタスク量から、月間のAPI費用を概算したい。
指示: 指定した1日あたりの処理件数・平均トークン数から、月間の入出力トークン数とコストを試算して。
出力形式: 前提条件・計算式・月間コスト概算（Gemini/Claude両方）の3段構成で出力して。
```

### 43. 既存ワークフローの分業ポイント診断

```text
前提: 今のClaude Code中心のワークフローに、Geminiを組み込む余地があるか知りたい。
指示: 指定したワークフローの各ステップを確認し、単純作業・大量処理に該当する箇所を抽出して。
出力形式: ステップ名・分業候補か否か・理由の表で出力して。
```

### 44. 処理速度がボトルネックのタスク特定

```text
前提: 一連の作業のどこで時間がかかっているか把握したい。
指示: 指定した処理ステップごとの所要時間データから、速度改善の余地が大きいステップを特定して。
出力形式: ステップ名・所要時間・改善余地（高中低）・改善案1行の表で出力して。
```

### 45. 年内価格と2027年価格の比較試算

```text
前提: Gemini 3.7 Flashの導入価格が2027年に値上げされることを踏まえて検討したい。
指示: 指定した月間利用量をもとに、2026年内の価格と2027年以降の価格でコストを比較して。
出力形式: 項目・2026年内コスト・2027年以降コスト・差額の表で出力して。
```

### 46. 複数モデル併用時の品質チェックリスト作成

```text
前提: 複数モデルを併用すると、成果物の品質にばらつきが出ないか心配。
指示: 指定したワークフローについて、モデルを切り替えた箇所での品質確認ポイントをリスト化して。
出力形式: 確認ポイントを箇条書きで10項目以上、各項目に確認方法を1行添えて。
```

### 47. 既存プロンプトのコスト最適化提案

```text
前提: 今使っているプロンプトが、必要以上に長い/複雑かもしれない。
指示: 指定したプロンプトを確認し、トークン数を削減できる余地がないか分析して。
出力形式: 削減できそうな箇所・削減理由・修正後のプロンプト案の3段構成で出力して。
```

### 48. 失敗リトライ率からの信頼性比較

```text
前提: どちらのモデルがリトライ（やり直し）が少なく済むか知りたい。
指示: 指定した過去の実行ログから、モデルごとのリトライ発生率を集計して。
出力形式: モデル名・総実行数・リトライ発生数・発生率の表で出力して。
```

### 49. チーム内の役割分担ルール文書化

```text
前提: チームメンバーによってGemini/Claudeの使い分け基準がバラバラ。
指示: 指定したタスク種別一覧をもとに、チーム共通の使い分けルールを文書化して。
出力形式: タスク種別・推奨モデル・理由を持つルール表と、例外時の判断フローを分けて出力して。
```

### 50. 導入効果の振り返りレポート雛形作成

```text
前提: 分業体制を試した後、効果があったか振り返りたい。
指示: 指定した導入前後の指標（コスト・時間・品質）をもとに、振り返りレポートの雛形を作成して。
出力形式: 導入前→導入後の比較表と、次の一手についての提案を3つ、分けて出力して。
```

## Antigravity検証・比較系

### 51. Claude CodeとAntigravityの機能比較表作成

```text
前提: 両方のツールの違いを一度きちんと整理したい。
指示: 指定した機能項目リスト（コード生成・ブラウザ操作・非同期実行等）ごとに、両ツールの対応状況を調べて。
出力形式: 機能名・Claude Code・Antigravityの対応状況（◎○△×）を持つ表で出力して。不明点は要調査と明記して。
```

### 52. 同一タスクの実行結果比較記録シート作成

```text
前提: 同じタスクを両方のツールに実行させて比較検証したい。
指示: 指定したタスク内容をもとに、比較記録用のシート（実行時間・成果物の質・つまずいた点）の雛形を作成して。
出力形式: 記入欄付きの表形式で、両ツール分を左右に並べて出力して。
```

### 53. 移行検討時のチェックリスト作成

```text
前提: 一部の作業をAntigravityに移すかどうか検討している。
指示: 指定した現在のワークフローをもとに、移行前に確認すべき項目をチェックリスト化して。
出力形式: 実践チェックリスト型（■ステップ→□判定項目→→YES:行動）で、最低8項目出力して。
```

### 54. 併用時のファイル競合リスク洗い出し

```text
前提: 同じプロジェクトを両方のツールで触ると、ファイルが競合しないか心配。
指示: 指定したプロジェクト構成をもとに、複数ツール併用時に起きうる競合パターンを洗い出して。
出力形式: 競合パターン・発生条件・回避策の3列で表にまとめて。
```

### 55. 学習コストの比較まとめ

```text
前提: チームメンバーがAntigravityを新たに習得する場合のコスト感を知りたい。
指示: 指定した公式ドキュメントの情報をもとに、Claude Code経験者がAntigravityを学ぶ際のポイントをまとめて。
出力形式: 「すでに知っている概念」「新しく覚える概念」の2カテゴリに分けて箇条書きで出力して。
```

### 56. 検証用の小規模プロジェクト設計

```text
前提: 本番導入前に、小さく試せるプロジェクトを用意したい。
指示: 指定した業務内容をもとに、1週間程度で検証できる小規模プロジェクトの計画を立てて。
出力形式: 目的・検証項目・完了条件・所要日数の4項目で構成して出力して。
```

### 57. 導入判断のための質問リスト作成

```text
前提: チームでAntigravity導入を検討する会議の準備をしている。
指示: 指定したチームの状況をもとに、導入判断に必要な論点を質問形式でリスト化して。
出力形式: 質問文を10個以上、カテゴリ（コスト/品質/学習コスト/セキュリティ）ごとに分けて出力して。
```

### 58. 既存Claude Code資産の移行可否チェック

```text
前提: 今使っているCLAUDE.md・スキル・エージェント設定がAntigravityでも使えるか知りたい。
指示: 指定した設定ファイルの内容を確認し、Antigravity側で近い機能があるか調べて。
出力形式: 現在の設定項目・Antigravity側の相当機能・移行難易度（易中難）の表で出力して。
```

### 59. 両ツール併用時のコミット履歴整理案

```text
前提: 複数ツールで作業すると、コミットメッセージの書き方がバラバラになりそう。
指示: 指定したプロジェクトのコミット規約をもとに、ツールが変わっても統一感を保つルールを提案して。
出力形式: 現行ルール・追加すべき記載（使用ツール名等）・例文の3段構成で出力して。
```

### 60. 比較検証の結果を発信用構成案に落とし込む

```text
前提: 検証結果を社内共有・発信用にまとめたい。
指示: 指定した比較検証結果をもとに、共有資料用に「結論→根拠→注意点」の順で構成案を作って。
出力形式: 見出しと箇条書きのみのMarkdown構成案（本文は書かない）で出力して。
```

以上、全60件です。


### 3. Gemini 3.7 Flash×Claude Code 分業ワークフロー設計図30（ワークフロー）
# 請求書処理・記事量産・開発保守など、具体的な業務でGemini 3.7 FlashとClaude Codeをどう分業させるかのレシピ集です。3カテゴリ×各10本、全30本。

# 🎁 Gemini 3.7 Flash×Claude Code 分業ワークフロー設計図30

請求書処理・記事量産・開発保守など、具体的な業務でGemini 3.7 FlashとClaude Codeをどう分業させるかのレシピ集です。3カテゴリ×各10本、全30本。

**受け取り方**: 下の各コードブロック右上のコピーボタンでコピーするか、このファイルをそのままダウンロードしてください。

**使い方**: 自分の業務に近いレシピを探し、フロー欄をそのまま手順書やスクリプト設計のたたき台として使ってください。

## 定型書類処理系レシピ

### 1. 請求書処理の分業フロー

```text
月末に届く請求書PDFの処理を、抽出はGemini・仕訳判断はClaudeに分ける流れ。
① Gemini 3.7 Flashが請求書PDF全件から金額・発行元・期日を抽出
② 抽出結果をClaude Codeに渡し、勘定科目の仕訳判断と例外検知を実施
③ 仕訳済みデータを経理システム形式で出力
④ 人間が最終確認してから確定させる
PDF抽出はルール化しやすく大量にあるためGeminiが得意。仕訳判断は文脈理解が必要なためClaudeに残す。
```

### 2. 契約書レビューの一次仕分け

```text
大量の契約書から、要注意な条項を含むものだけをClaudeで精査する流れ。
① Geminiが契約書PDF全件から条項の有無（自動更新・違約金等）を機械的に抽出
② 要注意フラグが立ったものだけClaude Codeが精読しリスク評価
③ 評価結果を優先度順にまとめて法務担当へ共有
全件を人力精読する時間を削減しつつ、重要判断はClaudeの読解力に委ねる設計。
```

### 3. 経費精算の一次チェック

```text
経費申請の形式チェックをGemini、例外判断をClaudeが担う流れ。
① Geminiが領収書画像とフォームの記入内容を突き合わせ、金額不一致や記入漏れを検出
② 検出された例外ケースだけClaude Codeが規定と照らして判断
③ 承認可否の一次案を経理担当に提示
大量の定型チェックはGeminiで高速化し、規定解釈が必要な例外だけ人の判断に近いClaudeへ。
```

### 4. 採用応募書類の一次スクリーニング

```text
大量の応募書類から、必須条件を満たすものだけを抽出する流れ。
① Geminiが応募書類PDF全件から必須項目（経験年数・資格等）の有無を機械的に判定
② 条件を満たした候補者についてClaude Codeが職務経歴の文脈を読み、適性コメントを作成
③ 人事担当へ絞り込み結果とコメントを提出
機械的なフィルタリングと、文脈理解が必要な評価コメントを明確に分ける設計。
```

### 5. アンケート自由記述の集計フロー

```text
大量の自由記述回答から、傾向をつかみやすくする流れ。
① Geminiが全回答をポジティブ/ネガティブ/中立に一次分類
② 各カテゴリの代表的な回答をClaude Codeが要約し、示唆をまとめる
③ 経営会議用のレポートとして整形
大量分類はGemini、意味のある示唆の言語化はClaudeという役割分担。
```

### 6. 在庫棚卸しデータの照合

```text
大量の在庫データと帳簿を突き合わせる作業の分業。
① Geminiが在庫システムのデータと帳簿データを突き合わせ、差異のある品目を抽出
② 差異の原因についてClaude Codeが過去ログと照らして仮説を立てる
③ 現場担当者への確認依頼リストを作成
大量データの突き合わせは高速なGemini、原因推測はClaudeの推論力を活用。
```

### 7. 会員規約変更の影響範囲調査

```text
規約変更が既存の各種ドキュメントにどう影響するか調べる流れ。
① Geminiが社内の全ドキュメントから、変更対象の条項に言及している箇所を検索・抽出
② 抽出箇所についてClaude Codeが影響度を評価し、修正が必要なドキュメントを判定
③ 修正タスクリストを作成
全文検索的な大量抽出はGemini、影響評価という判断業務はClaudeに残す設計。
```

### 8. 多言語問い合わせの一次対応

```text
海外からの問い合わせを、言語判定と一次回答で分業する流れ。
① Geminiが問い合わせメールの言語判定と定型的な一次回答文の下書きを作成
② 込み入った内容や交渉が絡む場合はClaude Codeが文面を精査・修正
③ 担当者が最終確認して送信
定型応答の量産はGemini、ニュアンスが重要な文面はClaudeが仕上げる。
```

### 9. 社内規程集の整合性チェック

```text
複数ある社内規程の間で矛盾がないか確認する流れ。
① Geminiが全規程ファイルから、同一テーマ（休暇・経費等）に触れている箇所を横断抽出
② 抽出箇所間の矛盾や重複についてClaude Codeが精査し指摘事項をまとめる
③ 総務担当へ改訂提案として提出
横断的な大量テキスト抽出をGeminiに任せ、論理的な整合性判断はClaudeに委ねる。
```

### 10. 監査対応資料の一次収集

```text
監査で求められる資料を大量のフォルダから集める作業の分業。
① Geminiが指定フォルダ群から、監査要求事項に該当しそうなファイルを機械的に収集
② 収集結果についてClaude Codeが要求事項との対応関係を確認し、不足を洗い出す
③ 監査担当へ提出リストと不足リストを提示
大量ファイルからの一次収集はGemini、要求事項との照合判断はClaudeに残す。
```

## コンテンツ量産系レシピ

### 11. 記事下書きの量産フロー

```text
複数トピックの記事下書きを、下書きはGemini・仕上げはClaudeで分ける流れ。
① Geminiが指定したトピックリストごとに、構成案と本文下書きを高速生成
② Claude Codeが下書きを読み、トーン・論理展開・事実確認を仕上げる
③ 最終稿を公開用フォーマットに整形
量産部分の速度とコストをGeminiで抑え、品質を左右する仕上げにClaudeの時間を使う。
```

### 12. SNS投稿文の大量バリエーション生成

```text
同じ告知内容から、複数プラットフォーム向けの投稿文を量産する流れ。
① Geminiが告知内容から、X・Instagram・LinkedIn向けにそれぞれ複数パターンの文面を生成
② Claude Codeがブランドトーンに沿っているか確認し、微調整
③ 投稿スケジュールに合わせて配信予約
パターン量産はGemini、ブランドの一貫性チェックはClaudeが担当。
```

### 13. プレゼン資料の構成初稿生成

```text
説明資料のたたき台となる構成案を高速に作る流れ。
① Geminiがテーマとキーポイントから、章立てと各章の要点を生成
② Claude Codeが構成の流れ・説得力の強さを見直し、練り直す
③ 資料として最終化
初稿のスピードをGeminiで確保し、読み手を惹きつける構成判断はClaudeが担当。
```

### 14. メルマガ配信文の量産フロー

```text
週次メルマガの複数案を作り比較検討したい場合の流れ。
① Geminiが同じ告知内容から件名・本文の複数パターンを生成
② Claude Codeが開封率・読みやすさの観点で有力案を選定・調整
③ 配信システムに登録
案出しの物量はGemini、選定という判断業務はClaudeに委ねる。
```

### 15. FAQページの下書き一括生成

```text
製品マニュアルから、FAQを大量に下書きする流れ。
① Geminiがマニュアル全体から想定される質問と回答の下書きを大量生成
② Claude Codeが重複整理・優先順位付け・トーン統一を実施
③ FAQページとして公開
大量下書きの生成はGemini、情報整理と品質担保はClaudeが担う。
```

### 16. 商品説明文のトーン別量産

```text
同じ商品を複数のトーン（フォーマル/カジュアル等）で説明する流れ。
① Geminiが商品スペックから、複数トーンの説明文をそれぞれ生成
② Claude Codeがブランドガイドラインとの整合性を確認し調整
③ 販売チャネルごとに使い分けて掲載
トーン違いのバリエーション生成という量産作業をGeminiに任せる。
```

### 17. プレスリリース初稿の高速作成

```text
発表内容から、プレスリリースのたたき台を作る流れ。
① Geminiが発表内容の骨子から、報道向けの初稿を生成
② Claude Codeが誇張表現のチェックと事実確認を行い修正
③ 広報担当が最終承認
初稿の速度はGemini、正確性と表現リスクの管理はClaudeが担当。
```

### 18. Q&Aコンテンツの大量翻訳

```text
既存の日本語Q&Aを複数言語に展開する流れ。
① Geminiが既存Q&A全件を対象言語に一括翻訳
② Claude Codeが専門用語や文化的なニュアンスのズレを確認・修正
③ 各言語版のページとして公開
翻訳の物量処理はGemini、ニュアンス調整という難所はClaudeに残す。
```

### 19. 社内報コンテンツの下書き量産

```text
複数部署からの寄稿ネタを、記事の形に整える流れ。
① Geminiが部署ごとのメモ書きから、記事形式の下書きを生成
② Claude Codeが社内報全体としてのトーン統一と読みやすさを調整
③ 編集担当が最終確認
バラバラな素材からの下書き生成はGemini、全体の一貫性はClaudeが担う。
```

### 20. チュートリアル記事の複数バージョン作成

```text
初心者向け・上級者向けなど、レベル別の解説記事を作る流れ。
① Geminiが同じ手順から、レベル別（初心者/中級/上級）の説明文をそれぞれ生成
② Claude Codeが各レベルで説明の粒度が適切か確認・調整
③ レベル別ページとして公開
レベル違いのバリエーション生成をGeminiに任せ、適切さの判断はClaudeに委ねる。
```

## 開発・保守系レシピ

### 21. 大量ファイルの一括リント修正下書き

```text
既存コードの軽微な指摘事項を大量に直す作業の分業。
① Geminiがリントツールの指摘事項に対する機械的な修正案を大量生成
② Claude Codeが修正案をレビューし、ロジックに影響する変更が無いか確認
③ 承認された分だけ適用
定型的な修正案の量産はGemini、安全性の最終判断はClaudeが担う。
```

### 22. テストケースの大量下書き作成

```text
カバレッジが低い箇所に、まずテストの叩き台を用意する流れ。
① Geminiが対象関数群から、正常系・異常系のテストケース下書きを大量生成
② Claude Codeが本当に意味のあるテストかを精査し、重複や無意味なケースを削除
③ 精査済みテストをリポジトリに追加
テストケースの物量確保はGemini、テストの質の担保はClaudeが担当。
```

### 23. APIドキュメントの自動下書き生成

```text
コードからAPIドキュメントのたたき台を作る流れ。
① Geminiがコード内の関数シグネチャとコメントから、ドキュメントの下書きを生成
② Claude Codeが実際の挙動と照らして正確性を確認・修正
③ 公式ドキュメントとして公開
下書きの速度はGemini、正確性の担保はClaudeという分業。
```

### 24. 大量の依存ライブラリ更新調査

```text
多数の依存パッケージについて、更新の影響を調べる流れ。
① Geminiが依存パッケージ全件のリリースノートを収集・要約
② Claude Codeが破壊的変更の有無とプロジェクトへの影響を判断
③ 更新計画を作成
大量情報の収集・要約はGemini、影響判断という難易度の高い作業はClaudeに残す。
```

### 25. コミット履歴からの変更履歴文書化

```text
大量のコミットから、リリースノートを作る流れ。
① Geminiが指定期間の全コミットメッセージを分類・要約
② Claude Codeがユーザー向けに分かりやすい表現へ書き直す
③ リリースノートとして公開
大量ログの一次整理はGemini、読み手を意識した表現調整はClaudeが担う。
```

### 26. バグ報告の一次トリアージ

```text
大量に届くバグ報告を、優先度順に並べる流れ。
① Geminiが報告内容から再現手順・影響範囲の有無を機械的に判定
② Claude Codeが深刻度と優先度を判断し、担当者を割り振る
③ トリアージ結果をチケット化
定型的な情報抽出はGemini、優先度という判断業務はClaudeに委ねる。
```

### 27. 大量のログイン画面キャプチャ比較

```text
デザイン変更の前後で、大量ページのスクリーンショットを比較する流れ。
① Geminiが前後のスクリーンショットを機械的に比較し、差分がある箇所を検出
② Claude Codeが意図した変更か意図しない崩れかを判断
③ 修正が必要な箇所をチケット化
画像レベルの機械比較はGemini、意図判断という文脈理解はClaudeが担う。
```

### 28. 設定ファイルの大量環境差分チェック

```text
複数環境（開発/検証/本番）の設定差分を確認する流れ。
① Geminiが各環境の設定ファイルを機械的に突き合わせ、差分をリスト化
② Claude Codeが意図した差分か設定ミスかを判断
③ 修正が必要な項目を担当者へ共有
差分抽出という機械的作業はGemini、意図判断はClaudeに残す設計。
```

### 29. 大量Issueのラベル自動提案

```text
溜まったIssueにラベルを付け直す作業の分業。
① Geminiが全Issueの本文からカテゴリ・優先度のラベル候補を機械的に提案
② Claude Codeが提案の妥当性を確認し、微妙なケースだけ再判断
③ 確定したラベルを一括適用
大量分類の一次案はGemini、境界線上の判断はClaudeに任せる。
```

### 30. 開発環境セットアップ手順の自動検証

```text
READMEのセットアップ手順が実際に動くか確認する流れ。
① Geminiが手順書の各コマンドを機械的に抽出し、実行ログを収集
② Claude Codeが失敗箇所の原因を特定し、手順書の修正案を作成
③ READMEを更新
大量コマンドの実行と収集はGemini、原因特定という推論はClaudeが担当。
```

以上、全30件です。

# 【世界一簡単】AI猛者が切り替えている最強AI " Codex " を初心者〜上級者でも分かりやすく徹底解説しました — プレゼント一式
# 2026-07-29 配布 / 3点
# ==========================================================


### 1. Codex神プラグイン・スキル逆引き手引き（早見表・逆引き）
# 「こういう時、何を使えばいいんだっけ？」を解消する25項目の逆引きガイドです。シーン別に該当するプラグイン・スキル・機能と、そのまま使える呼び出し方をまとめました。

# 🎁 Codex神プラグイン・スキル逆引き手引き

「こういう時、何を使えばいいんだっけ？」を解消する25項目の逆引きガイドです。シーン別に該当するプラグイン・スキル・機能と、そのまま使える呼び出し方をまとめました。

**受け取り方**: 各カードのボタンでコピー、または全件ダウンロードしてください。GitHub上ではこのファイルのコードブロックから直接コピーできます。

**使い方**: 該当するシーンを探して、そのままCodexに貼り付けるか、案内された機能を有効化してください。

## 開発・実装を助ける機能（1〜7）

### 1. 開発の進め方そのものが分からない時
説明: エンジニアの型を借りて開発を進める
```text
このプロジェクトを、経験豊富なエンジニアの標準的な進め方(要件確認→設計→実装→テスト)で進めてほしいです。関連するスキルがあれば使って進めて。
```

### 2. ライブラリのバージョンが古いかもしれない時
説明: Context7で最新ドキュメントを参照する
```text
このライブラリを使う前に、最新の公式ドキュメントを参照して、私が知っている情報と食い違いがないか確認してから実装を進めて。
```

### 3. 要件をうまく言葉にできない時
説明: 壁打ち系スキルにインタビューしてもらう
```text
作りたいものはあるのですが、うまく言葉にできません。1つずつ質問しながら要件を明確にしていくやり方で進めて。
```

### 4. コードを直接触らずWebページの中身を見たい時
説明: Firecrawlでページを構造化データ化する
```text
このURLの内容を、AIが読みやすい構造化された形式で取得して、要点を教えて。
```

### 5. 作ったアプリをすぐ公開したい時
説明: Vercelプラグインでワンコマンドデプロイ
```text
このWebアプリをインターネット上に公開したいです。デプロイして、共有できるURLを発行して。
```

### 6. 手元でしか動かないアプリを人に見せたい時
説明: ローカル/公開の違いを整理してもらう
```text
今このアプリは自分のパソコンの中でしか見られない状態ですか？もしそうなら、誰でも見られる状態にする方法を教えて、進めて。
```

### 7. 開発とは違う、自分のアプリにAI機能を足したい時
説明: App Server/SDKの違いを判断してもらう
```text
自作アプリにAIチャットのような機能を組み込みたいです。契約中のプラン内で収める方法と、別途API課金が必要な方法、それぞれのメリット・デメリットを教えて。
```

## 繰り返し作業を任せる機能（8〜14）

### 8. 同じ操作を何度も手作業でやっている時
説明: Record & Replayで操作を録画してスキル化する
```text
今から手順を録画で見せます。この後同じ作業を頼むときは、録画した内容を再現できるスキルとして作成して。
```

### 9. 毎日・毎週決まった時間にやっていることがある時
説明: オートメーションズで定期実行を組む
```text
この作業を毎週月曜の朝に自動で実行してほしいです。まず仕組みを説明してから、実行内容を質問して設定して。
```

### 10. 複雑な作業を1人に丸投げすると質が落ちる時
説明: サブエージェントで役割分担する
```text
この作業は、調査担当・実装担当・レビュー担当に分けて、それぞれサブエージェントとして並列に進めて。
```

### 11. 画面の状態をいちいち言葉で説明するのが面倒な時
説明: アップショットで画面ごと渡す
```text
今開いているこの画面を見て、状況を判断して次の対応を教えて。説明は省略します。
```

### 12. 自分のWebサイトの動作確認を手作業でしている時
説明: ブラウザユーズでE2E的に自動確認する
```text
このサイトのお問い合わせフォームを、実際に入力から送信まで操作して、正しく動くか確認して。
```

### 13. パソコンの中の単純作業を任せたい時
説明: コンピューターユーズで画面操作そのものを任せる
```text
このアプリの中の不要な項目を、実際に画面を操作して削除して。手順は任せます。
```

### 14. 定期実行が思ったタイミングで動かない時
説明: ローカル起動必須という制約を疑う
```text
設定したはずの定期実行が動いていません。パソコンがスリープしていた可能性はありますか？原因と対策を教えて。
```

## 情報収集・整理を助ける機能（15〜19）

### 15. 最新ニュースを毎回自分で探すのが面倒な時
説明: オートメーション+Web検索で自動収集する
```text
毎朝、AI関連の最新ニュースを5件集めて要約する作業を自動化して。
```

### 16. たくさんのメールやチャットを見落としがちな時
説明: プラグイン連携で優先度を自動判定する
```text
未読のメッセージの中から、返信が必要なものだけを優先度順にリストにして教えて。
```

### 17. 会議前に関連資料を探す時間がない時
説明: カレンダー+ドライブ連携で準備リストを作る
```text
今週の予定を確認して、それぞれの会議に必要そうな関連資料をドライブから探して、準備リストにして。
```

### 18. 社内特有のツールと連携したい時
説明: 既存プラグインで足りるか、MCP自作が必要か判断する
```text
社内独自のシステムと連携したいのですが、既存のプラグインで対応できますか？できなければ、必要な対応方法を教えて。
```

### 19. 情報源が信頼できるか不安な時
説明: 確度を分けて報告させる
```text
この情報について、公式で確認できるもの、一次情報はあるが未確定のもの、推測にすぎないものを分けて教えて。
```

## コンテンツ・資料制作を助ける機能（20〜25）

### 20. 文字起こしをうまく活用できていない時
説明: 1つの素材から複数アウトプットを作る
```text
この文字起こしを元に、要約・記事・SNS投稿案の3種類を一気に作って。
```

### 21. スライドや資料のデザインを毎回イチから考えている時
説明: テンプレートクリエイターで型を作る
```text
このデザインシステムを使って、パワーポイント・ワード・エクセルのテンプレートをそれぞれ作って。次からはこれを使い回したいです。
```

### 22. 画像を使った資料が作れないと思っている時
説明: 画像生成込みで資料そのものを作らせる
```text
このテーマについて、画像生成を使ったインフォグラフィック中心のスライドを2ページ作って。
```

### 23. 動画の内容を文章資産にしたい時
説明: 動画URLからブログ記事化する
```text
このYouTube動画のURLを渡すので、内容を確認して最適な構成でブログ記事を作って。
```

### 24. SNS投稿までまとめて任せたい時
説明: アプリ内ブラウザでログイン済みのまま投稿する
```text
このテーマについて図付きの投稿文を作って、アプリ内ブラウザでログイン済みのアカウントからそのまま投稿して。
```

### 25. どの機能を使えばいいか、そもそも迷った時
説明: 目的だけ伝えて機能選定ごと任せる
```text
やりたいことは〇〇です。ここまでのCodexの機能の中で、どれを組み合わせるのが最適か提案して、そのまま進めて。
```

## まとめ

以上、25項目です。内訳：開発・実装を助ける機能7／繰り返し作業を任せる機能7／情報収集・整理を助ける機能5／コンテンツ・資料制作を助ける機能6。


### 2. Codex実践プロンプト&スキル100選（プロンプト集）
# Codexを「基本操作」から「サブエージェント」「自動化」「Claude Codeからの移行」まで、10ジャンル×そのまま貼って試せる100本にまとめました。

# 🎁 Codex実践プロンプト&スキル100選

Codexを「基本操作」から「サブエージェント」「自動化」「Claude Codeからの移行」まで、10ジャンル×そのまま貼って試せる100本にまとめました。

**受け取り方**: 各カードのボタンでコピー、または全件ダウンロードしてください。GitHub上ではこのファイルのコードブロックから直接コピーできます。

**使い方**: カテゴリから気になるものを選び、そのままCodex(デスクトップアプリ／CLI／IDE拡張)のチャット欄に貼り付けてください。

## まずはチャットで試す・基本操作（1〜10）

### 1. プロジェクトなしで軽く試す
説明: フォルダを決めずにサクッと1つ作業を頼む
```text
プロジェクトは指定しないので、このままチャットで簡単なテキストファイルを1つ作って、中身に今日の日付と「Codexテスト」という文字を入れて保存して。
```

### 2. 既存フォルダをプロジェクトにする
説明: 手元のフォルダをそのままプロジェクト化する
```text
このフォルダをプロジェクトとして扱ってください。まずフォルダの中身をざっと確認して、何が入っているか3行で教えて。
```

### 3. AGENTS.mdを作らせる
説明: プロジェクト固有の前提をCodexに覚えさせる
```text
このプロジェクトの内容を確認した上で、Codexが次回以降このフォルダで作業するときに読み込む AGENTS.md を作成して。プロジェクトの目的・よく使うコマンド・注意点を含めて。
```

### 4. プランモードで要件を詰める
説明: いきなり実装せず、先に計画だけ立てさせる
```text
プランモードで進めます。まだ実装はせず、要件が曖昧な点があれば箇条書きで質問してください。すべて回答したらプランを提示して、私が承認してから実装に進んでください。
```

### 5. サイドチャットで質問だけする
説明: メインの作業を止めずに別枠で聞く
```text
今の作業は止めずに、サイドチャットで質問です。今実行しているコマンドが何をしているか、専門用語を使わずに説明して。
```

### 6. 権限モードを確認してから進める
説明: フルアクセスか自動レビューかを都度選ぶ習慣づけ
```text
これから実行する作業は、ファイルの削除を含みますか？含む場合は、実行前に何を削除するか一覧で見せてから確認を取ってください。
```

### 7. 公式ドキュメントを読み込んで要約させる
説明: 新機能が出たときの理解を早める
```text
Codexの公式ドキュメントの中から「サブエージェント」に関するページを探して読み込み、初心者にもわかるように3つのポイントに要約して。
```

### 8. 完了通知だけオンにして裏で走らせる
説明: 手を離しても終わったタイミングがわかるようにする
```text
これから頼む作業は完了までしばらくかかると思います。完了したら通知してください。作業中は他のことをしているので、途中経過は聞きません。
```

### 9. モデルと推論の深さを目的に合わせて選ぶ
説明: 簡単な作業は速く、重要な設計は深く考えさせる
```text
これはちょっとしたテキスト整形なので、モデルは標準、推論の深さも標準でお願いします。速度優先で進めてください。
```

### 10. Codexペットに今の状況を聞く
説明: バックグラウンド作業の進捗を軽く確認する
```text
今バックグラウンドで動いている作業は、今どの工程まで進んでいますか？ペット経由で一言で教えて。
```

## 画像生成を使いこなす（11〜18）

### 11. サクッとポスター画像を作る
説明: チャットのイメージ機能で1枚絵を作る
```text
GPT Imageの画像生成機能を使って、「AIエージェント入門セミナー」の告知ポスターを1枚作って。明るい配色で、参加者を募集している雰囲気にして。
```

### 12. 画像生成込みでスライド資料を作る
説明: PPTXの中に生成画像を埋め込ませる
```text
Codexの概要を説明する2ページ分のスライド資料(PPTX)を作って。文字数は少なめにして、画像生成で作ったインフォグラフィック中心の見せ方にして。
```

### 13. 画像から使う機能を切り替える
説明: 用途別に画像モデルの世代を指定する
```text
今回の画像生成は品質より速度を優先したいので、軽量モデル寄りの設定で生成して。ラフなイメージ確認用です。
```

### 14. 自分専用のCodexペットを作る
説明: Hatch Petスキルでキャラクターを作らせる
```text
Codex用のオリジナルペットを作りたいです。テーマは「読書好きなふくろう」。既存のペットと同じ雰囲気のドット絵スタイルで作って、設定まで完了させて。
```

### 15. 画像を繋いだLP風ページを作る
説明: 画像生成を組み合わせてグラフィカルなページにする
```text
画像生成を使って、美容系通販サイトのようなグラフィカルなランディングページを1枚作って。商品名は「AI活用スターターキット」にして。
```

### 16. ゲーム用のスプライト画像を作る
説明: 簡単なゲーム開発の下準備をする
```text
簡単な2Dゲームを作る前段階として、主人公キャラクターのドット絵スプライトを画像生成で作って。横向き・正面・後ろ向きの3方向分お願いします。
```

### 17. 画像生成の結果をレビューさせる
説明: 生成後にAI自身へ改善点を確認させる
```text
さっき生成した画像を見て、文字のかすれや不自然な部分がないか自己レビューして、あれば修正案を教えて。
```

### 18. 資料の図解だけ画像で差し替える
説明: 文章中心の資料に図解を後付けする
```text
今あるこの資料のうち、文章だけで分かりにくい部分を1箇所選んで、そこだけ説明用の図解画像を生成して差し込んで。
```

## スキルを作る・呼び出す（19〜30）

### 19. スキルクリエイターでゼロから作る
説明: 繰り返す作業をスキル化する入り口
```text
スキルクリエイターを使ってスキルを作りたいです。やりたいことは「YouTube用の企画メモをもらったら、3行要約と改善案をセットで返す」です。
```

### 20. マニュアル作成スキルを作る
説明: 操作の様子を録画してマニュアル化する
```text
今から画面録画で操作手順を見せます。それをもとに、画像付きの手順書をNotion上に新規ページとして保存するスキルを作って。
```

### 21. 要件を壁打ちしてくれるスキルを使う
説明: 曖昧な依頼を対話で具体化する
```text
まだ何を作るか固まっていません。要件を1つずつ質問しながら固めていくやり方で、対象・目的・完成条件を明確にしてください。
```

### 22. スキルの中身を確認する
説明: 既存スキルのSKILL.mdを読んで理解する
```text
今使えるスキル一覧の中から1つ選んで、そのSKILL.mdの中身を見せて、どういう手順で動くスキルか説明して。
```

### 23. スキルを軽量に保つよう頼む
説明: SKILL.mdが長くなりすぎるのを防ぐ
```text
今作っているスキルのSKILL.mdが長くなってきていませんか？もし長ければ、指示の核となる部分だけ残してコンパクトにまとめ直して。
```

### 24. 既存のスキルを別プロジェクトに移す
説明: フォルダをまたいでスキルを使い回す
```text
このスキルを、別のプロジェクトフォルダでも使えるように、共有できる形式で書き出して。
```

### 25. イベント告知バナー生成スキルを作る
説明: 画像+概要文+SNS投稿文を1発で作る
```text
セミナー名・料金・開催日を伝えたら、告知用バナー画像・概要文・SNS投稿文の3点を同時に作ってくれるスキルを作って。背景画像とロゴは固定で使い回したいです。
```

### 26. 定型レポート作成スキルを作る
説明: 決まったフォーマットの資料を型化する
```text
週次の営業レポートを、決まった見出し構成(概要・数字・課題・来週の予定)で毎回同じ形式で作れるスキルにして。
```

### 27. スキル同士を組み合わせて使う
説明: 複数スキルを1つの依頼でまとめて呼び出す
```text
スキルAで下調べをして、その結果をスキルBの資料作成に渡す、という2段階の流れで一気に進めて。
```

### 28. スキルの動作をテストする
説明: 作ったスキルが期待通りか確認する
```text
さっき作ったスキルを、実際のサンプルデータで一度動かしてみて、想定外の挙動がないか確認して。
```

### 29. スキルを受け取ってセットアップする
説明: 他人から共有されたスキルを導入する
```text
このZIPファイルは知人から受け取ったCodexのスキルです。私のCodex環境にセットアップしてください。
```

### 30. スキルの棚卸しをする
説明: 使っていないスキルを整理する
```text
今登録されているスキルを一覧にして、それぞれ何をするものか一言で説明して。使っていなさそうなものがあれば教えて。
```

## プラグイン・MCP連携（31〜42）

### 31. Notionと接続してテスト投稿する
説明: プラグイン接続の動作確認をする
```text
Notionのプラグインを接続してください。接続できたら、指定したページの配下に「テスト投稿」というタイトルのページを1つ作って動作確認して。
```

### 32. 最新ドキュメントを参照させる
説明: Context7で古い知識のズレを防ぐ
```text
このライブラリを使った実装をする前に、最新の公式ドキュメントを参照してから進めて。学習データが古い可能性があるので、バージョンのズレがないか確認して。
```

### 33. Webページを構造化データとして取得する
説明: Firecrawlでスクレイピングを丸投げする
```text
このURLのページ内容を、AIが読みやすい構造化された形式(見出し・本文・リンク)で取得して、要点を教えて。
```

### 34. アプリをワンコマンドでデプロイする
説明: Vercelプラグインで公開まで任せる
```text
今手元で動いているこのWebアプリを、Vercelにデプロイして、誰でも見られるURLを発行して。
```

### 35. 操作を録画してスキル化する
説明: Record & Replayで繰り返し作業を自動化する
```text
今から30分以内でこの一連の操作を録画します。録画が終わったら、その内容から再利用できるスキルを作成して。
```

### 36. 開いている画面をそのまま渡す
説明: アップショットで説明の手間を省く
```text
今開いているこの画面を見て、どこにエラーが出ているか教えて。画面はそのまま渡すので、状況を説明する必要はありません。
```

### 37. アプリ内ブラウザで自分のサイトを操作する
説明: インアップブラウザで直接フィードバックする
```text
アプリ内ブラウザでこのURLを開いて、お問い合わせフォームの入力テストをしてください。名前とメールアドレスは適当なテストデータで構いません。
```

### 38. どのプラグインを使うべきか相談する
説明: 目的からプラグイン選びを任せる
```text
Googleカレンダーの予定を確認して、関連資料をドライブから探して準備リストを作りたいです。どのプラグインを組み合わせればいいか教えて、そのまま進めて。
```

### 39. 新しいプラグインを探して提案してもらう
説明: プラグイン一覧から目的に合うものを探す
```text
プラグイン一覧を確認して、スプレッドシートの自動更新に使えそうなものがあれば教えて。
```

### 40. MCPサーバーを自作するか判断してもらう
説明: 既存プラグインで足りるかを見極める
```text
社内の独自システムと連携したいのですが、既存のプラグインで対応できますか？できない場合、MCPサーバーを自作する必要があるか教えて。
```

### 41. プラグインの権限を最小限に絞る
説明: 必要な処理だけに権限を付与する
```text
このプラグインに与える権限を確認して、今回の用途(閲覧のみ)に対して権限が広すぎないか教えて。広すぎる場合は絞り込んで。
```

### 42. 複数プラグインの動作を一括確認する
説明: 接続済みプラグインの棚卸しをする
```text
今接続しているプラグインを一覧にして、それぞれ最後に使ったのがいつか分かる範囲で教えて。使っていないものがあれば教えて。
```

## サブエージェントで分担する（43〜52）

### 43. レビュー専門のサブエージェントを立てる
説明: 品質チェックだけを任せる担当を作る
```text
この実装が終わったら、セキュリティとバグの観点だけをチェックする専門のサブエージェントを立てて、レビューさせて。
```

### 44. 調査・構成・執筆を役割分担させる
説明: ライティング作業をチーム制にする
```text
この記事を書くにあたって、リサーチ担当・構成担当・執筆担当・レビュー担当の4役に分けて、それぞれサブエージェントとして並列に動かして。
```

### 45. 並列でA案・B案を作らせて比較する
説明: 選択肢を同時に作らせて見比べる
```text
デザイン案をA案とB案の2パターン、それぞれ別のサブエージェントに並列で作らせて、できたら両方を並べて見せて。
```

### 46. サブエージェントの数を絞って使う
説明: トークン消費を抑えつつ分担する
```text
今回はそこまで複雑ではないので、サブエージェントは最大2体までに絞って進めて。
```

### 47. サブエージェントごとにチャットを分けて確認する
説明: 何が起きているか個別に見えるようにする
```text
今動いている各サブエージェントの進捗を、それぞれ別のチャットとして見られるようにして、今どこまで進んだか教えて。
```

### 48. 最終的な結果だけメインにまとめさせる
説明: 各担当の途中経過はノイズにしない
```text
各サブエージェントの調査結果は、途中の試行錯誤は省いて、結論だけをメインのエージェントにまとめて報告させて。
```

### 49. 探索作業をサブエージェントに任せる
説明: 読み込み中心のタスクを並列化する
```text
このコードベース全体を確認して、〇〇に関連するファイルをすべて洗い出す作業をサブエージェントに任せて、私はその間別の作業をします。
```

### 50. サブエージェントの推論レベルを個別に設定する
説明: 担当ごとに賢さと速度のバランスを変える
```text
バグ調査担当のサブエージェントだけ推論レベルを高めにして、他の軽い作業担当は標準のままにして。
```

### 51. 人間チームの分担をそのままAIに当てはめる
説明: 会議の役割分担の考え方を応用する
```text
会議でよくやるように、議題ごとに担当を分けるやり方で、この作業も分野ごとにサブエージェントへ振り分けて進めて。
```

### 52. サブエージェントの結果に矛盾がないか確認する
説明: 分担後の整合性チェックを忘れない
```text
複数のサブエージェントが出した結果に、矛盾している部分がないか最後に確認して、あれば指摘して。
```

## オートメーション(自動化)を組む（53〜64）

### 53. チャットで自動化を設定する
説明: 対話しながらオートメーションを組む
```text
自動化を設定したいです。まずCodexの自動化の仕組みを説明してから、私がいつ何をしてほしいか答えるので、質問してください。
```

### 54. 毎朝のニュースまとめを自動化する
説明: 定期実行の入り口として一番使いやすい例
```text
毎朝8時に、AI関連の最新ニュースを5件集めて、初心者にもわかるように要約したものを教えてください。この作業を自動化して。
```

### 55. 未返信メールの優先度リストを作る
説明: 見落としを防ぐ自動チェック
```text
毎日夕方、その日届いたメールの中で返信が必要なのに返信していないものを優先度付きでリストにして教えて。この確認を自動化して。
```

### 56. 週次レポートをスラック/チャットに送る
説明: 情報整理の型を仕組み化する
```text
毎週月曜9時に、先週のスラックで自分宛のメンションと重要な決定事項をまとめて、カテゴリー別に整理したレポートを送って。これを自動化して。
```

### 57. 自動化の実行環境を指定する
説明: ローカル/クラウドどちらで動かすか意識する
```text
この自動化は、パソコンを閉じていても動いてほしいです。ローカル実行とクラウド実行、どちらが適しているか教えて、適した方で設定して。
```

### 58. 自動化前にまず手動で1回試す
説明: いきなり丸投げにしない安全な進め方
```text
この作業を自動化する前に、まず今すぐ1回だけ手動で実行してみて、結果に問題がないか確認させて。
```

### 59. 出力形式を明確に指定して自動化する
説明: 平均的な結果に収束させないための一言
```text
自動化するレポートの出力形式は、マークダウンではなくWebページ(URL)として届けてほしいです。この前提で設定して。
```

### 60. 自動化の実行結果に通知を付ける
説明: 終わったことに気づけるようにする
```text
この定期実行が完了したら、必ず完了通知を送るように設定して。
```

### 61. 自動化の権限を必要最小限にする
説明: 裏で動く処理だからこそ慎重に
```text
この自動化は裏で動くので、ファイルの削除など取り返しのつかない操作は含めないでください。読み取りと通知だけに限定して。
```

### 62. 既存の自動化を一覧で確認する
説明: 増えてきた自動化を棚卸しする
```text
今設定されている自動化(オートメーション)を一覧にして、それぞれいつ何をしているか教えて。
```

### 63. 自動化をクラウド実行に切り替える
説明: ローカル起動必須の制約を回避する選択肢を検討する
```text
この自動化はローカルのパソコンが起動している必要がありますか？もしそうなら、クラウド実行や外部サービス連携で代替できないか、選択肢とコスト感を教えて。
```

### 64. 自動化の頻度を調整する
説明: 多すぎる通知を整理する
```text
この自動化、毎日実行だと通知が多すぎるので、平日だけに変更して。
```

## 資料・ドキュメント作成（65〜74）

### 65. 文字起こしから複数のアウトプットを作る
説明: 1つの素材から要約・記事・投稿文を量産する
```text
この文字起こしを元に、要約・note記事・X投稿案(5本)の3種類のアウトプットを作って。
```

### 66. Excelから報告スライドを自動生成する
説明: 数字を渡すだけでプレゼン資料にする
```text
このExcelの売上データを分析して、グラフ付きの報告スライドを作って。デザインは整えて、来週の会議でそのまま使える形にして。
```

### 67. 動画URLからブログ記事を作る
説明: 動画コンテンツをテキスト資産に変換する
```text
このYouTube動画のURLを渡すので、内容を確認して、最適な構成でブログ記事を作って。必要な図解や画像も適宜入れて。
```

### 68. 資料をテンプレート化する
説明: 毎回同じデザインで作れるようにする
```text
このデザインシステム(配色・ロゴ)を使って、Word・PowerPoint・Excelそれぞれのテンプレートを作って。次からはこのテンプレートを使い回したいです。
```

### 69. 議事録から次のアクションを抽出する
説明: 会議メモをそのまま放置しない
```text
この打ち合わせメモを読んで、誰が・いつまでに・何をするかのアクションリストを作って。
```

### 70. 読みやすさ優先で専門用語をかみ砕く
説明: 初心者向けに文章を書き直させる
```text
この文章、専門用語が多くて分かりにくいので、AI初心者でも読めるように、たとえ話を交えて書き直して。
```

### 71. 資料の一部だけ図解に差し替える
説明: 文章だらけの資料にメリハリをつける
```text
この資料のうち、比較の部分を表に、流れの部分をステップ図に変えて、視覚的にわかりやすくして。
```

### 72. 出典・確度を明記させる
説明: 断定と推測を区別してもらう
```text
この資料に書かれている数字や主張について、公式で確認できるもの・一次情報はあるが未確定のもの・推測にすぎないものを分けて、印を付けて。
```

### 73. 資料のTL;DRを作る
説明: 最初に3行で結論を伝える
```text
この資料を最初に読む人のために、冒頭に「まず3行で分かる結論」を追加して。
```

### 74. 複数資料を1つに統合する
説明: バラバラの資料をまとめ直す
```text
この3つの資料は内容が重複しています。重複を整理して、1つの資料にまとめ直して。
```

## アプリ・Web開発（75〜86）

### 75. アイデアだけでアプリを作り始める
説明: 要件を対話で詰めながら作る
```text
文字起こしを貼ったら要約・記事・SNS投稿を自動生成してくれるWebアプリを作りたいです。要件が曖昧な部分は質問してください。
```

### 76. 業務自動化のスキルをアプリ化する
説明: 繰り返し作業をアプリの形で仕組み化する
```text
週次のAIニュースをまとめてスラックに送る一連の流れを、スキルとオートメーションを使って仕組み化して。
```

### 77. 自作アプリにCodexの体験を埋め込む
説明: App Serverでサブスク課金内に収める
```text
このアプリの中に、Codexのようなチャット型のAI体験を埋め込みたいです。API課金ではなく契約中のプラン内で収まる方法を教えて。
```

### 78. 作りかけの画面をその場で直す
説明: インアップブラウザで見ながら修正する
```text
今作っているこのページを開いて、ボタンの配置が崩れている部分を直接見ながら直して。
```

### 79. 動作確認をブラウザ操作で自動化する
説明: E2Eテストをブラウザユーズに任せる
```text
このWebアプリのフォーム入力からエラー表示までの一連の動作を、実際にブラウザを操作して確認して、崩れがあれば直して。
```

### 80. デプロイ後にログを確認させる
説明: 公開後の不具合にすぐ気づけるようにする
```text
このアプリをデプロイした後、エラーログが出ていないか確認して、問題があれば教えて。
```

### 81. 簡単なゲームを作ってみる
説明: 画像生成と組み合わせて遊びで作る
```text
簡単なミニゲームを1つ作ってみたいです。ジャンルは何でもいいので、まずどんなゲームが作りやすいか3つ提案して。
```

### 82. アプリの環境変数を安全に設定する
説明: APIキー等を画面越しに直接見せずに扱う
```text
このアプリに必要なAPIキーの設定方法を教えてください。私が値を入力する場所だけ案内して、値自体は表示しないでください。
```

### 83. 既存アプリのUIだけ改善する
説明: ロジックはそのままに見た目だけ整える
```text
このアプリの機能は変えずに、見た目だけもう少し洗練された印象にしてください。
```

### 84. モバイル表示を確認して直す
説明: スマホでの見え方をチェックする
```text
このページをスマホの画面幅で見たときに崩れている箇所がないか確認して、あれば直して。
```

### 85. 複数チャットで並列開発する
説明: 独立した機能を同時並行で作る
```text
このプロジェクトの中で、記事Aを作るチャットと記事Bを作るチャットを分けて、それぞれ独立して並行で進めて。
```

### 86. アプリ完成後にREADMEを作る
説明: 後から見返せるようにドキュメント化する
```text
このアプリが完成したので、何を作ったか・使い方・注意点をまとめたREADMEを作って。
```

## 音声入力・リモート操作（87〜94）

### 87. 音声入力でざっくり指示を出す
説明: タイピングより速く要件を伝える
```text
今から音声入力で要件を話すので、聞き取った内容を整理して、要件定義としてまとめ直して。
```

### 88. 辞書登録で専門用語の誤変換を防ぐ
説明: よく使う固有名詞を正しく認識させる
```text
音声入力でよく誤変換される単語(サービス名・固有名詞)を辞書登録したいので、登録の仕方を教えて。
```

### 89. スマホから作業の続きを確認する
説明: リモートコネクションズで外出先から見る
```text
今パソコンで進めている作業の進捗を、外出先のスマホから確認したいです。設定方法を教えて。
```

### 90. スマホから新しいチャットを始める
説明: 外出先で思いついたことをすぐ頼む
```text
スマホから新しいチャットを開いて、さっき思いついた〇〇の調査を今から頼みたいです。
```

### 91. 常時起動マシンとの接続を確認する
説明: リモート運用の前提を点検する
```text
自宅の常時起動しているパソコンとの接続状態を確認して、今すぐ作業を投げられる状態か教えて。
```

### 92. 通知設定を最適化する
説明: 見逃しなく、かつ通知過多にならないようにする
```text
作業が完了したタイミングだけ通知が来るように、通知の設定を整理して。
```

### 93. どの端末からでも続けられる作業を選ぶ
説明: リモート向きの作業とそうでない作業を切り分ける
```text
今頼んでいる一連の作業のうち、リモートから続けやすいものとパソコンの前でやった方がいいものを分けて教えて。
```

### 94. 音声入力とプランモードを組み合わせる
説明: 話しながら要件を固めていく
```text
音声入力で要望を話すので、それをプランモードの要件としてまとめて、足りない情報があれば質問して。
```

## Claude Codeからの移行（95〜100）

### 95. Claude Codeの設定をそのまま持ってくる
説明: 0から作り直さず移行する
```text
Claude Codeで使っていた設定・MCPサーバー・コマンドをそのままCodexにインポートしたいです。手順を教えて、進めて。
```

### 96. CLAUDE.mdの内容をAGENTS.mdに移す
説明: 同じ役割のファイルに置き換える
```text
このCLAUDE.mdの内容を、Codex用のAGENTS.mdとして同じ意味を保ったまま書き直して。
```

### 97. 同じ作業を両方で試して比較する
説明: 乗り換え判断のための実地比較
```text
この同じ作業を、Claude CodeとCodexの両方で試して、スピード・出力の質・使いやすさを比較して教えて。
```

### 98. 使い分けの基準を整理してもらう
説明: 全部乗り換えず併用する前提で考える
```text
自分の作業内容を伝えるので、Claude CodeとCodex、どちらを使うべきか、使い分けの基準を提案して。
```

### 99. 移行後に困った点を洗い出す
説明: 乗り換え直後のつまずきを早めに解消する
```text
Codexに乗り換えてみて、Claude Codeと勝手が違って戸惑っている点があるので、それぞれの解決方法を教えて。
```

### 100. 両方のいいとこ取りの運用を考える
説明: 1つのプロジェクトで両方動かす
```text
同じフォルダの中で、Claude CodeとCodexの両方を状況に応じて使い分けたいです。運用ルールの叩き台を作って。
```

## まとめ

以上、100個です。内訳：まずはチャットで試す・基本操作10／画像生成を使いこなす8／スキルを作る・呼び出す12／プラグイン・MCP連携12／サブエージェントで分担する10／オートメーション(自動化)を組む12／資料・ドキュメント作成10／アプリ・Web開発12／音声入力・リモート操作8／Claude Codeからの移行6。


### 3. Claude Code→Codex 乗り換え判断チェックリスト（チェックリスト）
# → YES: Codexへの乗り換え、または併用を検討する余地が大きい

【Claude Code→Codex 乗り換え判断チェックリスト】

■ ステップ1：料金面で判断する
□ 今、月20ドル前後のプランで利用制限にすぐ達してしまっているか
→ YES: Codexへの乗り換え、または併用を検討する余地が大きい
□ すでに月100〜200ドルクラスの上位プランを契約しているか
→ YES: 料金差は縮まるため、機能面での比較を優先する
指示例: 今の自分の使用量から、Claude CodeとCodexそれぞれの料金プランでどちらが割安になるか試算して。

■ ステップ2：得意分野で判断する
□ 主な作業がコーディング・デザイン性の高いWeb制作か
→ YES: Claude Codeの強みが活きやすいので、そのまま比較検討する
□ 主な作業が資料作成・業務自動化・非エンジニア向けの実務か
→ YES: Codexの実行力・画像生成・プラグインの幅が向いている可能性が高い
指示例: 自分がよく頼む作業内容を伝えるので、Claude CodeとCodexどちらが向いているか判断して。

■ ステップ3：利用制限・安定性で判断する
□ 直近1週間で「制限に達して使えない」体験が2回以上あったか
→ YES: 乗り換え・併用を試す優先度は高い
□ 特に不便を感じていないか
→ YES: 無理に乗り換える必要はない。まずは併用から試す
指示例: 直近の利用状況を振り返って、制限に達した回数や場面を教えて。

■ ステップ4：チーム・運用面で判断する
□ チームメンバーもClaude Codeに慣れているか
→ YES: 全員一斉の乗り換えは避け、まず自分だけで試す
□ 個人利用が中心で、乗り換えのハードルが低いか
→ YES: 今回のプロンプト・スキル100選を使って早速試す
指示例: チームの利用状況を整理して、乗り換えるとしたら誰から始めるのが良いか提案して。

■ ステップ5：移行のハードルを確認する
□ CLAUDE.mdやMCPサーバーなど、移行が面倒そうな設定があるか
→ YES: インポート機能でそのまま持ち込めるか確認する
□ 特に複雑な設定はしていないか
→ YES: 移行のハードルは低いので、すぐに試して問題ない
指示例: 今使っているClaude Codeの設定・MCPサーバー・コマンドをCodexにそのままインポートできるか確認して、進めて。

■ ステップ6：併用という選択肢を検討する
□ どちらか一方に絞らず、案件ごとに使い分けたいか
→ YES: 同じフォルダで両方使う運用ルールを決めておく
□ シンプルに1つに統一したいか
→ YES: ここまでのステップの判定結果をもとに、どちらか1つを選ぶ
指示例: 同じプロジェクトの中でClaude CodeとCodexを使い分けたいので、運用ルールの叩き台を作って。
